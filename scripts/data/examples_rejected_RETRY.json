[
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def twoSum(self, nums: List[int], target: int) -> List[int]:\n        d = {}\n        for i, x in enumerate(nums):\n            y = target - x\n            if y in d:\n                return [i, d[y]]\n            d[x] = i\n        return []",
    "bug_explanation": "The student returns [i, d[y]] instead of [d[y], i], which reverses the order. Since d[y] is an earlier index and i is the current index, the return should be [d[y], i] to maintain the correct index order (earlier index first). For nums=[2,7,11,15], target=9, this returns [1,0] instead of [0,1].",
    "socratic_hint": "Your logic for finding the pair is correct, but check the order of indices in your return statement. Which index was encountered first during your iteration, and which should appear first in the result?",
    "test_input": "[2,7,11,15], target=9",
    "problem_id": 1,
    "problem_title": "Two Sum",
    "problem_description": "Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target.\nYou may assume that each input would have exactly one solution, and you may not use the same element twice.\nYou can return the answer in any order.\n \nExample 1:\n\nInput: nums = [2,7,11,15], target = 9\nOutput: [0,1]\nExplanation: Because nums[0] + nums[1] == 9, we return [0, 1].\n\nExample 2:\n\nInput: nums = [3,2,4], target = 6\nOutput: [1,2]\n\nExample 3:\n\nInput: nums = [3,3], target = 6\nOutput: [0,1]\n\n \nConstraints:\n\n2 <= nums.length <= 104\n-109 <= nums[i] <= 109\n-109 <= target <= 109\nOnly one valid answer exists.\n\n \nFollow-up: Can you come up with an algorithm that is less than O(n2) time complexity?",
    "problem_solution": "class Solution:\n    def twoSum(self, nums: List[int], target: int) -> List[int]:\n        d = {}\n        for i, x in enumerate(nums):\n            if (y := target - x) in d:\n                return [d[y], i]\n            d[x] = i\n",
    "difficulty": "Easy",
    "tags": [
      "Array",
      "Hash Table"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The code is not actually buggy for the standard Two Sum specification: returning indices in either order is accepted, so [1,0] is still a valid answer.",
        "The claimed bug (index order) is not a real correctness issue under typical problem requirements, so the hint targets a non-bug and may mislead students."
      ],
      "suggested_improvement": "Use a genuinely failing variant (e.g., storing d[x]=i before checking y in d, which can break cases like nums=[3,3], target=6, or returning values instead of indices). Then craft a Socratic hint that points to the check/store order or what is being returned (indices vs values) without stating the exact fix."
    }
  },
  {
    "error_type": "off_by_one",
    "buggy_code": "class Solution:\n    def removeDuplicates(self, nums: List[int]) -> int:\n        k = 1\n        for i in range(1, len(nums)):\n            if nums[i] != nums[i - 1]:\n                nums[k] = nums[i]\n                k += 1\n        return k",
    "bug_explanation": "The student initializes k to 1 instead of 0, assuming the first element is already in place. However, when the array has only one element, or when all elements are duplicates, this causes the function to skip placing the first element at position 0 and returns an incorrect count. For example, with nums = [1], it returns 1 but doesn't ensure nums[0] is set correctly in the general flow of the algorithm.",
    "socratic_hint": "Your code starts counting from one instead of zero. What happens when you test your function with an array containing just a single element? Does your initialization assume something about the first position that might not always hold true?",
    "problem_id": 26,
    "problem_title": "Remove Duplicates From Sorted Array",
    "problem_description": "Given an integer array nums sorted in non-decreasing order, remove the duplicates in-place such that each unique element appears only once. The relative order of the elements should be kept the same. Then return the number of unique elements in nums.\nConsider the number of unique elements of nums to be k, to get accepted, you need to do the following things:\n\nChange the array nums such that the first k elements of nums contain the unique elements in the order they were present in nums initially. The remaining elements of nums are not important as well as the size of nums.\nReturn k.\n\nCustom Judge:\nThe judge will test your solution with the following code:\n\nint[] nums = [...]; // Input array\nint[] expectedNums = [...]; // The expected answer with correct length\n\nint k = removeDuplicates(nums); // Calls your implementation\n\nassert k == expectedNums.length;\nfor (int i = 0; i < k; i++) {\n    assert nums[i] == expectedNums[i];\n}\n\nIf all assertions pass, then your solution will be accepted.\n \nExample 1:\n\nInput: nums = [1,1,2]\nOutput: 2, nums = [1,2,_]\nExplanation: Your function should return k = 2, with the first two elements of nums being 1 and 2 respectively.\nIt does not matter what you leave beyond the returned k (hence they are underscores).\n\nExample 2:\n\nInput: nums = [0,0,1,1,1,2,2,3,3,4]\nOutput: 5, nums = [0,1,2,3,4,_,_,_,_,_]\nExplanation: Your function should return k = 5, with the first five elements of nums being 0, 1, 2, 3, and 4 respectively.\nIt does not matter what you leave beyond the returned k (hence they are underscores).\n\n \nConstraints:\n\n1 <= nums.length <= 3 * 104\n-100 <= nums[i] <= 100\nnums is sorted in non-decreasing order.\n\n",
    "problem_solution": "class Solution:\n    def removeDuplicates(self, nums: List[int]) -> int:\n        k = 0\n        for x in nums:\n            if k == 0 or x != nums[k - 1]:\n                nums[k] = x\n                k += 1\n        return k\n",
    "difficulty": "Easy",
    "tags": [
      "Array",
      "Two Pointers"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The provided code is a standard correct solution for LeetCode 26 when the input array is non-empty; initializing k=1 is correct under that constraint.",
        "The bug explanation claims incorrect behavior for single-element or all-duplicate arrays, but the function returns the correct k and leaves nums[0] unchanged (already correct).",
        "The hint targets an off-by-one initialization issue that does not actually cause failure under the problem's constraints, so it doesn't match a real bug."
      ],
      "suggested_improvement": "Use a genuinely buggy variant (e.g., start the loop at i=0 and compare nums[i] to nums[i-1], or initialize k=0 and write nums[k]=nums[i] without handling the first element), and adjust the hint to probe the failing edge case (like empty input if allowed, or the first comparison/write position)."
    }
  },
  {
    "error_type": "wrong_algorithm",
    "buggy_code": "class Solution:\n    def majorityElement(self, nums: List[int]) -> int:\n        counts = {}\n        for num in nums:\n            counts[num] = counts.get(num, 0) + 1\n        max_count = 0\n        result = 0\n        for num, count in counts.items():\n            if count > max_count:\n                max_count = count\n                result = num\n        return result",
    "bug_explanation": "While this approach would work for positive numbers, it initializes result to 0, which causes incorrect output when 0 is not in the array but negative numbers are, or when a negative number is the majority element but appears with the same frequency as another element initially. For example, with nums=[-1], it would return 0 instead of -1.",
    "socratic_hint": "You're tracking counts correctly, but look at how you initialize your result variable. What happens if the array contains only negative numbers or doesn't contain the value you initialized with? Could this lead to returning something that wasn't even in the input?",
    "problem_id": 169,
    "problem_title": "Majority Element",
    "problem_description": "Given an array nums of size n, return the majority element.\nThe majority element is the element that appears more than ⌊n / 2⌋ times. You may assume that the majority element always exists in the array.\n \nExample 1:\nInput: nums = [3,2,3]\nOutput: 3\nExample 2:\nInput: nums = [2,2,1,1,1,2,2]\nOutput: 2\n\n \nConstraints:\n\nn == nums.length\n1 <= n <= 5 * 104\n-109 <= nums[i] <= 109\n\n \nFollow-up: Could you solve the problem in linear time and in O(1) space?",
    "problem_solution": "class Solution:\n    def majorityElement(self, nums: List[int]) -> int:\n        cnt = m = 0\n        for x in nums:\n            if cnt == 0:\n                m, cnt = x, 1\n            else:\n                cnt += 1 if m == x else -1\n        return m\n",
    "difficulty": "Easy",
    "tags": [
      "Array",
      "Hash Table",
      "Divide and Conquer",
      "Counting",
      "Sorting"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The code is not actually buggy: initializing result=0 does not matter because the loop over counts.items() will always set result to some key from the input (counts is non-empty for valid Majority Element inputs). For nums=[-1], it returns -1, not 0.",
        "The bug explanation and hint focus on an initialization issue that does not cause incorrect output, so the hint does not match a real bug and is not helpful for fixing anything."
      ],
      "suggested_improvement": "Replace with a genuinely failing implementation (e.g., an off-by-one in counting, incorrect majority threshold, or a broken Boyer-Moore update), and then craft a Socratic hint that targets that specific failure mode."
    }
  },
  {
    "error_type": "edge_case",
    "buggy_code": "class Solution:\n    def majorityElement(self, nums: List[int]) -> int:\n        cnt = 0\n        m = None\n        for x in nums:\n            if cnt == 0:\n                m = x\n                cnt = 1\n            else:\n                cnt += 1 if m == x else -1\n        return m",
    "bug_explanation": "The student initializes m to None instead of 0. While this seems logical, it causes issues because when cnt becomes 0 again and m is reassigned, the very first check 'if cnt == 0' happens before m gets a value in iteration 1, so m is initially None. This actually works correctly in most cases, but the inconsistency with the reference solution's initialization pattern could cause issues in edge cases or type checking scenarios. More critically, the logic is correct so this isn't actually a bug. Let me revise.",
    "socratic_hint": "INVALID - This actually works correctly",
    "problem_id": 169,
    "problem_title": "Majority Element",
    "problem_description": "Given an array nums of size n, return the majority element.\nThe majority element is the element that appears more than ⌊n / 2⌋ times. You may assume that the majority element always exists in the array.\n \nExample 1:\nInput: nums = [3,2,3]\nOutput: 3\nExample 2:\nInput: nums = [2,2,1,1,1,2,2]\nOutput: 2\n\n \nConstraints:\n\nn == nums.length\n1 <= n <= 5 * 104\n-109 <= nums[i] <= 109\n\n \nFollow-up: Could you solve the problem in linear time and in O(1) space?",
    "problem_solution": "class Solution:\n    def majorityElement(self, nums: List[int]) -> int:\n        cnt = m = 0\n        for x in nums:\n            if cnt == 0:\n                m, cnt = x, 1\n            else:\n                cnt += 1 if m == x else -1\n        return m\n",
    "difficulty": "Easy",
    "tags": [
      "Array",
      "Hash Table",
      "Divide and Conquer",
      "Counting",
      "Sorting"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": false,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The provided code is a correct implementation of Boyer-Moore majority vote and does not fail on valid inputs, so it is not actually buggy.",
        "Claimed error type and bug explanation are incorrect/misleading (initializing m to None is fine here).",
        "The hint is not Socratic and provides no guidance; it just declares the example invalid.",
        "Because there is no real bug, the hint cannot match an actual bug."
      ],
      "suggested_improvement": "Replace this example with genuinely buggy code (e.g., incorrect counter update or missing candidate reset) and provide a Socratic hint that prompts the student to trace what happens when the count drops to zero and the next element differs from the current candidate."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "# Definition for a binary tree node.\n# class TreeNode:\n#     def __init__(self, val=0, left=None, right=None):\n#         self.val = val\n#         self.left = left\n#         self.right = right\nclass Solution:\n    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:\n        if root is None:\n            return None\n        root.left, root.right = root.right, root.left\n        self.invertTree(root.left)\n        self.invertTree(root.right)\n        return root",
    "bug_explanation": "The student swaps the children first, then recursively inverts what are now the swapped children. This means after swapping at the root, when they recursively call on root.left, they're actually inverting the original right subtree (which is now on the left), and vice versa. This effectively swaps twice at each level, resulting in the tree not being fully inverted. For example, with [4,2,7,1,3,6,9], the immediate children get swapped to [4,7,2,...] but then the recursive calls work on the wrong subtrees, leading to incorrect final structure.",
    "socratic_hint": "Think about the order of operations in your recursive approach. After you swap the children at the current node, which subtrees are you then recursively processing? Are they the same subtrees you started with, or have they moved?",
    "problem_id": 226,
    "problem_title": "Invert Binary Tree",
    "problem_description": "Given the root of a binary tree, invert the tree, and return its root.\n \nExample 1:\n\n\nInput: root = [4,2,7,1,3,6,9]\nOutput: [4,7,2,9,6,3,1]\n\nExample 2:\n\n\nInput: root = [2,1,3]\nOutput: [2,3,1]\n\nExample 3:\n\nInput: root = []\nOutput: []\n\n \nConstraints:\n\nThe number of nodes in the tree is in the range [0, 100].\n-100 <= Node.val <= 100\n\n",
    "problem_solution": "# Definition for a binary tree node.\n# class TreeNode:\n#     def __init__(self, val=0, left=None, right=None):\n#         self.val = val\n#         self.left = left\n#         self.right = right\nclass Solution:\n    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:\n        if root is None:\n            return None\n        l, r = self.invertTree(root.left), self.invertTree(root.right)\n        root.left, root.right = r, l\n        return root\n",
    "difficulty": "Easy",
    "tags": [
      "Tree",
      "Depth-First Search",
      "Breadth-First Search",
      "Binary Tree"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The provided code is a standard correct solution for inverting a binary tree: swapping children then recursively inverting the (now swapped) subtrees still produces the correct fully inverted tree.",
        "The bug explanation is incorrect (it claims a double-swap effect that does not occur), so the hint is targeting a non-existent bug and is not helpful for this code."
      ],
      "suggested_improvement": "Replace the buggy code with an actually incorrect variant (e.g., swap after the recursive calls, or recurse on saved original children but then swap incorrectly), and update the explanation/hint to match the real failure case."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "# Definition for a binary tree node.\n# class TreeNode:\n#     def __init__(self, val=0, left=None, right=None):\n#         self.val = val\n#         self.left = left\n#         self.right = right\nclass Solution:\n    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:\n        if root is None:\n            return None\n        self.invertTree(root.left)\n        self.invertTree(root.right)\n        root.left, root.right = root.right, root.left\n        return root",
    "bug_explanation": "The student recursively inverts the left and right subtrees first, but then swaps them at the parent level. This causes the tree to be inverted twice - once during the recursive calls (which invert the subtrees) and once during the swap. The net effect is that the tree structure remains mostly unchanged or incorrectly inverted. For example, the subtrees get inverted internally, but then the final swap at each level undoes or incorrectly applies the inversion.",
    "socratic_hint": "Trace through what happens at each node step by step. After the recursive calls complete and the subtrees are already inverted, what effect does your swap operation have? Does this produce the result you want, or does it cause an extra transformation?",
    "problem_id": 226,
    "problem_title": "Invert Binary Tree",
    "problem_description": "Given the root of a binary tree, invert the tree, and return its root.\n \nExample 1:\n\n\nInput: root = [4,2,7,1,3,6,9]\nOutput: [4,7,2,9,6,3,1]\n\nExample 2:\n\n\nInput: root = [2,1,3]\nOutput: [2,3,1]\n\nExample 3:\n\nInput: root = []\nOutput: []\n\n \nConstraints:\n\nThe number of nodes in the tree is in the range [0, 100].\n-100 <= Node.val <= 100\n\n",
    "problem_solution": "# Definition for a binary tree node.\n# class TreeNode:\n#     def __init__(self, val=0, left=None, right=None):\n#         self.val = val\n#         self.left = left\n#         self.right = right\nclass Solution:\n    def invertTree(self, root: Optional[TreeNode]) -> Optional[TreeNode]:\n        if root is None:\n            return None\n        l, r = self.invertTree(root.left), self.invertTree(root.right)\n        root.left, root.right = r, l\n        return root\n",
    "difficulty": "Easy",
    "tags": [
      "Tree",
      "Depth-First Search",
      "Breadth-First Search",
      "Binary Tree"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The provided code is a correct invert-binary-tree solution: inverting children first and then swapping still produces the correct inverted tree, so it does not fail any test case.",
        "The bug explanation is incorrect (there is no 'double inversion' that cancels out).",
        "Because there is no real bug, the hint does not match an actual issue and is unlikely to help."
      ],
      "suggested_improvement": "Replace the buggy code with a genuinely incorrect variant (e.g., swap before recursion but recurse on the original child references, or forget to swap at all), then adjust the explanation and hint to target that real failure mode."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def isPowerOfTwo(self, n: int) -> bool:\n        if n <= 0:\n            return False\n        while n > 1:\n            if n % 2 == 1:\n                return False\n            n = n / 2\n        return True",
    "bug_explanation": "The student uses regular division (/) instead of integer division (//). In Python 3, regular division returns a float. After the first division, n becomes a float like 8.0, and the comparison n % 2 == 1 may not work as expected due to floating point representation. More critically, the loop continues infinitely for some values because n becomes a float that never reaches exactly 1.",
    "socratic_hint": "Think about what type of value your division operation produces in Python 3. When you repeatedly divide by two, does the variable remain an integer or become something else? How might this affect your loop condition and comparisons?",
    "problem_id": 231,
    "problem_title": "Power Of Two",
    "problem_description": "Given an integer n, return true if it is a power of two. Otherwise, return false.\nAn integer n is a power of two, if there exists an integer x such that n == 2x.\n \nExample 1:\n\nInput: n = 1\nOutput: true\nExplanation: 20 = 1\n\nExample 2:\n\nInput: n = 16\nOutput: true\nExplanation: 24 = 16\n\nExample 3:\n\nInput: n = 3\nOutput: false\n\n \nConstraints:\n\n-231 <= n <= 231 - 1\n\n \nFollow up: Could you solve it without loops/recursion?",
    "problem_solution": "class Solution:\n    def isPowerOfTwo(self, n: int) -> bool:\n        return n > 0 and (n & (n - 1)) == 0\n",
    "difficulty": "Easy",
    "tags": [
      "Bit Manipulation",
      "Recursion",
      "Math"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The code is not actually buggy for typical integer inputs: using / produces floats but powers of two remain exactly representable, the loop terminates, and modulo checks still behave correctly for these values.",
        "The bug explanation claims infinite loops and incorrect modulo behavior due to floating point, which is generally false here; the hint targets a non-issue rather than a demonstrable failing case."
      ],
      "suggested_improvement": "Use a genuinely failing bug (e.g., n = n / 2 combined with a strict integer-type check, or a different logical mistake), or adjust the explanation/hint to match an actual failure case. Alternatively, frame the issue as style/robustness (type drift) rather than a logic error, but then change the claimed error type and validation expectations accordingly."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def isPowerOfTwo(self, n: int) -> bool:\n        if n <= 0:\n            return False\n        return (n & n - 1) == 0",
    "bug_explanation": "Missing parentheses around (n - 1). Due to operator precedence, this evaluates as (n & n) - 1 == 0, which becomes n - 1 == 0, so it only returns True when n == 1. This fails for all other powers of 2 like 2, 4, 8, etc.",
    "socratic_hint": "Consider the order in which operations are evaluated in your bitwise expression. What gets computed first: the bitwise AND or the subtraction? Does this match what you intended to compare?",
    "problem_id": 231,
    "problem_title": "Power Of Two",
    "problem_description": "Given an integer n, return true if it is a power of two. Otherwise, return false.\nAn integer n is a power of two, if there exists an integer x such that n == 2x.\n \nExample 1:\n\nInput: n = 1\nOutput: true\nExplanation: 20 = 1\n\nExample 2:\n\nInput: n = 16\nOutput: true\nExplanation: 24 = 16\n\nExample 3:\n\nInput: n = 3\nOutput: false\n\n \nConstraints:\n\n-231 <= n <= 231 - 1\n\n \nFollow up: Could you solve it without loops/recursion?",
    "problem_solution": "class Solution:\n    def isPowerOfTwo(self, n: int) -> bool:\n        return n > 0 and (n & (n - 1)) == 0\n",
    "difficulty": "Easy",
    "tags": [
      "Bit Manipulation",
      "Recursion",
      "Math"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The code is not actually buggy in Python: '-' has higher precedence than '&', so (n & n - 1) is parsed as n & (n - 1), which is correct.",
        "The bug explanation (and thus the hint) assumes incorrect operator precedence, so it does not match an actual failure."
      ],
      "suggested_improvement": "Use a genuinely buggy variant (e.g., return (n & (n - 1)) == 1 or return (n | (n - 1)) == 0) and adjust the hint to target that real mistake, or keep the code and change the explanation/hint to focus on why the expression works in Python."
    }
  },
  {
    "error_type": "edge_case",
    "buggy_code": "class Solution:\n    def reverse(self, x: int) -> int:\n        ans = 0\n        mi, mx = -(2**31), 2**31 - 1\n        negative = x < 0\n        x = abs(x)\n        while x:\n            y = x % 10\n            ans = ans * 10 + y\n            x = x // 10\n        if negative:\n            ans = -ans\n        if ans < mi or ans > mx:\n            return 0\n        return ans",
    "bug_explanation": "This code converts the input to its absolute value to simplify the reversal logic, but checks for overflow AFTER computing the reversed number. This violates the constraint that we cannot store 64-bit integers. If x is a large negative number like -2147483648, reversing it might temporarily create a value outside the 32-bit range before the overflow check happens.",
    "socratic_hint": "You're checking whether the result fits within the allowed range, but think about when this check happens. If the reversed value is already too large, what might have already occurred in memory before you perform your validation? The problem states the environment doesn't allow storing values beyond a certain size.",
    "problem_id": 7,
    "problem_title": "Reverse Integer",
    "problem_description": "Given a signed 32-bit integer x, return x with its digits reversed. If reversing x causes the value to go outside the signed 32-bit integer range [-231, 231 - 1], then return 0.\nAssume the environment does not allow you to store 64-bit integers (signed or unsigned).\n \nExample 1:\n\nInput: x = 123\nOutput: 321\n\nExample 2:\n\nInput: x = -123\nOutput: -321\n\nExample 3:\n\nInput: x = 120\nOutput: 21\n\n \nConstraints:\n\n-231 <= x <= 231 - 1\n\n",
    "problem_solution": "class Solution:\n    def reverse(self, x: int) -> int:\n        ans = 0\n        mi, mx = -(2**31), 2**31 - 1\n        while x:\n            if ans < mi // 10 + 1 or ans > mx // 10:\n                return 0\n            y = x % 10\n            if x < 0 and y > 0:\n                y -= 10\n            ans = ans * 10 + y\n            x = (x - y) // 10\n        return ans\n",
    "difficulty": "Medium",
    "tags": [
      "Math"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The code produces correct outputs under Python's integer model and correctly returns 0 when the reversed value is outside 32-bit range; there is no failing test case for the stated LeetCode problem.",
        "The claimed bug relies on a language/environment constraint (cannot store >32-bit during computation) that is not applicable to Python and is not enforced by the problem's judge; thus the hint targets a non-bug.",
        "Hint is not helpful for this code because it pushes overflow-during-construction concerns that don't change correctness here."
      ],
      "suggested_improvement": "Use a genuinely failing bug (e.g., mishandling trailing zeros, incorrect sign handling, or incorrect overflow boundary check) and craft a hint that targets that specific incorrect behavior with a concrete example input/output mismatch."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def reverse(self, x: int) -> int:\n        ans = 0\n        mi, mx = -(2**31), 2**31 - 1\n        while x:\n            if ans < mi // 10 or ans > mx // 10:\n                return 0\n            y = x % 10\n            if x < 0 and y > 0:\n                y -= 10\n            ans = ans * 10 + y\n            x = (x - y) // 10\n        return ans",
    "bug_explanation": "The overflow check is incomplete. It checks if ans < mi // 10 or ans > mx // 10, but doesn't account for the case where ans equals exactly mi // 10 or mx // 10 and adding the next digit would cause overflow. For example, if ans is 214748364 (which equals mx // 10) and the next digit is 8 or 9, multiplying ans by 10 and adding the digit would exceed the max value.",
    "socratic_hint": "Your overflow check considers when the current accumulated value is far from the boundaries, but what happens when the value is exactly at the boundary divided by ten? If you're right at that threshold and add one more digit, could you still overflow even though your condition passed?",
    "problem_id": 7,
    "problem_title": "Reverse Integer",
    "problem_description": "Given a signed 32-bit integer x, return x with its digits reversed. If reversing x causes the value to go outside the signed 32-bit integer range [-231, 231 - 1], then return 0.\nAssume the environment does not allow you to store 64-bit integers (signed or unsigned).\n \nExample 1:\n\nInput: x = 123\nOutput: 321\n\nExample 2:\n\nInput: x = -123\nOutput: -321\n\nExample 3:\n\nInput: x = 120\nOutput: 21\n\n \nConstraints:\n\n-231 <= x <= 231 - 1\n\n",
    "problem_solution": "class Solution:\n    def reverse(self, x: int) -> int:\n        ans = 0\n        mi, mx = -(2**31), 2**31 - 1\n        while x:\n            if ans < mi // 10 + 1 or ans > mx // 10:\n                return 0\n            y = x % 10\n            if x < 0 and y > 0:\n                y -= 10\n            ans = ans * 10 + y\n            x = (x - y) // 10\n        return ans\n",
    "difficulty": "Medium",
    "tags": [
      "Math"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed overflow-check bug does not actually manifest: the code checks overflow before incorporating the next digit, and for boundary cases (ans == mx//10 or ans == mi//10) the next iteration will detect overflow after the update, returning 0 as required.",
        "Because the code is not actually buggy, the hint and bug explanation do not match a real failing behavior."
      ],
      "suggested_improvement": "Replace this example with code that truly fails on a concrete test (e.g., a version that never handles negative digits correctly, or an overflow check that is performed after updating ans without guarding, or one that omits the negative adjustment), and update the hint to target that real failure."
    }
  },
  {
    "error_type": "edge_case",
    "buggy_code": "class Solution:\n    def myAtoi(self, s: str) -> int:\n        if not s:\n            return 0\n        n = len(s)\n        if n == 0:\n            return 0\n        i = 0\n        while i < n and s[i] == ' ':\n            i += 1\n        if i == n:\n            return 0\n        sign = -1 if s[i] == '-' else 1\n        if s[i] in ['-', '+']:\n            i += 1\n        res = 0\n        while i < n:\n            if not s[i].isdigit():\n                break\n            c = int(s[i])\n            res = res * 10 + c\n            i += 1\n        res = sign * res\n        if res > 2**31 - 1:\n            return 2**31 - 1\n        if res < -(2**31):\n            return -(2**31)\n        return res",
    "bug_explanation": "The code performs overflow checking AFTER computing the entire result, which means for very large numbers like '20000000000000000000', the intermediate computation of res will overflow Python's integer limits (or in other languages cause undefined behavior). The correct approach is to check for overflow BEFORE multiplying by 10 and adding the next digit.",
    "socratic_hint": "Consider what happens when the input represents a very large number that exceeds the allowed range. At what point in your calculation do you check whether the result will overflow? Is it possible that the overflow has already occurred by that time?",
    "problem_id": 8,
    "problem_title": "String To Integer Atoi",
    "problem_description": "Implement the myAtoi(string s) function, which converts a string to a 32-bit signed integer.\nThe algorithm for myAtoi(string s) is as follows:\n\nWhitespace: Ignore any leading whitespace (\" \").\nSignedness: Determine the sign by checking if the next character is '-' or '+', assuming positivity if neither present.\nConversion: Read the integer by skipping leading zeros until a non-digit character is encountered or the end of the string is reached. If no digits were read, then the result is 0.\nRounding: If the integer is out of the 32-bit signed integer range [-231, 231 - 1], then round the integer to remain in the range. Specifically, integers less than -231 should be rounded to -231, and integers greater than 231 - 1 should be rounded to 231 - 1.\n\nReturn the integer as the final result.\n \nExample 1:\n\nInput: s = \"42\"\nOutput: 42\nExplanation:\n\nThe underlined characters are what is read in and the caret is the current reader position.\nStep 1: \"42\" (no characters read because there is no leading whitespace)\n         ^\nStep 2: \"42\" (no characters read because there is neither a '-' nor '+')\n         ^\nStep 3: \"42\" (\"42\" is read in)\n           ^\n\n\nExample 2:\n\nInput: s = \" -042\"\nOutput: -42\nExplanation:\n\nStep 1: \"   -042\" (leading whitespace is read and ignored)\n            ^\nStep 2: \"   -042\" ('-' is read, so the result should be negative)\n             ^\nStep 3: \"   -042\" (\"042\" is read in, leading zeros ignored in the result)\n               ^\n\n\nExample 3:\n\nInput: s = \"1337c0d3\"\nOutput: 1337\nExplanation:\n\nStep 1: \"1337c0d3\" (no characters read because there is no leading whitespace)\n         ^\nStep 2: \"1337c0d3\" (no characters read because there is neither a '-' nor '+')\n         ^\nStep 3: \"1337c0d3\" (\"1337\" is read in; reading stops because the next character is a non-digit)\n             ^\n\n\nExample 4:\n\nInput: s = \"0-1\"\nOutput: 0\nExplanation:\n\nStep 1: \"0-1\" (no characters read because there is no leading whitespace)\n         ^\nStep 2: \"0-1\" (no characters read because there is neither a '-' nor '+')\n         ^\nStep 3: \"0-1\" (\"0\" is read in; reading stops because the next character is a non-digit)\n          ^\n\n\nExample 5:\n\nInput: s = \"words and 987\"\nOutput: 0\nExplanation:\nReading stops at the first non-digit character 'w'.\n\n \nConstraints:\n\n0 <= s.length <= 200\ns consists of English letters (lower-case and upper-case), digits (0-9), ' ', '+', '-', and '.'.\n\n",
    "problem_solution": "class Solution:\n    def myAtoi(self, s: str) -> int:\n        if not s:\n            return 0\n        n = len(s)\n        if n == 0:\n            return 0\n        i = 0\n        while s[i] == ' ':\n            i += 1\n            # 仅包含空格\n            if i == n:\n                return 0\n        sign = -1 if s[i] == '-' else 1\n        if s[i] in ['-', '+']:\n            i += 1\n        res, flag = 0, (2**31 - 1) // 10\n        while i < n:\n            # 非数字，跳出循环体\n            if not s[i].isdigit():\n                break\n            c = int(s[i])\n            # 溢出判断\n            if res > flag or (res == flag and c > 7):\n                return 2**31 - 1 if sign > 0 else -(2**31)\n            res = res * 10 + c\n            i += 1\n        return sign * res\n",
    "difficulty": "Medium",
    "tags": [
      "String"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed bug (intermediate overflow) does not occur in Python because integers have arbitrary precision, so the code still returns the correct clamped value for very large inputs.",
        "Because the code is not actually buggy in Python, the hint targets a non-issue and does not match an actual failing behavior for this implementation."
      ],
      "suggested_improvement": "Use a genuinely failing edge case for this Python code (or change the language/context to a fixed-width integer language). If keeping Python, target a real Atoi-spec edge case and craft the hint around that specific failure."
    }
  },
  {
    "error_type": "off_by_one",
    "buggy_code": "class Solution:\n    def threeSum(self, nums: List[int]) -> List[List[int]]:\n        nums.sort()\n        n = len(nums)\n        ans = []\n        for i in range(n - 2):\n            if nums[i] > 0:\n                break\n            if i and nums[i] == nums[i - 1]:\n                continue\n            j, k = i + 1, n - 1\n            while j < k:\n                x = nums[i] + nums[j] + nums[k]\n                if x < 0:\n                    j += 1\n                elif x > 0:\n                    k -= 1\n                else:\n                    ans.append([nums[i], nums[j], nums[k]])\n                    j, k = j + 1, k - 1\n                    while j < k and nums[j] == nums[j - 1]:\n                        j += 1\n                    while j < k and nums[k] == nums[k - 1]:\n                        k -= 1\n        return ans",
    "bug_explanation": "The second while loop for skipping duplicates checks nums[k] == nums[k - 1] instead of nums[k] == nums[k + 1]. This causes the pointer to compare with the wrong adjacent element, failing to properly skip duplicate values on the right side.",
    "socratic_hint": "When you're moving the right pointer leftward to skip duplicates, which adjacent element should you compare with - the one you just came from, or the one you're moving away from? Think about the direction of movement.",
    "problem_id": 15,
    "problem_title": "3Sum",
    "problem_description": "Given an integer array nums, return all the triplets [nums[i], nums[j], nums[k]] such that i != j, i != k, and j != k, and nums[i] + nums[j] + nums[k] == 0.\nNotice that the solution set must not contain duplicate triplets.\n \nExample 1:\n\nInput: nums = [-1,0,1,2,-1,-4]\nOutput: [[-1,-1,2],[-1,0,1]]\nExplanation: \nnums[0] + nums[1] + nums[2] = (-1) + 0 + 1 = 0.\nnums[1] + nums[2] + nums[4] = 0 + 1 + (-1) = 0.\nnums[0] + nums[3] + nums[4] = (-1) + 2 + (-1) = 0.\nThe distinct triplets are [-1,0,1] and [-1,-1,2].\nNotice that the order of the output and the order of the triplets does not matter.\n\nExample 2:\n\nInput: nums = [0,1,1]\nOutput: []\nExplanation: The only possible triplet does not sum up to 0.\n\nExample 3:\n\nInput: nums = [0,0,0]\nOutput: [[0,0,0]]\nExplanation: The only possible triplet sums up to 0.\n\n \nConstraints:\n\n3 <= nums.length <= 3000\n-105 <= nums[i] <= 105\n\n",
    "problem_solution": "class Solution:\n    def threeSum(self, nums: List[int]) -> List[List[int]]:\n        nums.sort()\n        n = len(nums)\n        ans = []\n        for i in range(n - 2):\n            if nums[i] > 0:\n                break\n            if i and nums[i] == nums[i - 1]:\n                continue\n            j, k = i + 1, n - 1\n            while j < k:\n                x = nums[i] + nums[j] + nums[k]\n                if x < 0:\n                    j += 1\n                elif x > 0:\n                    k -= 1\n                else:\n                    ans.append([nums[i], nums[j], nums[k]])\n                    j, k = j + 1, k - 1\n                    while j < k and nums[j] == nums[j - 1]:\n                        j += 1\n                    while j < k and nums[k] == nums[k + 1]:\n                        k -= 1\n        return ans\n",
    "difficulty": "Medium",
    "tags": [
      "Array",
      "Two Pointers",
      "Sorting"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed bug (using nums[k] == nums[k - 1] instead of nums[k] == nums[k + 1]) is not actually a bug in this implementation because k is decremented before the duplicate-skip loop; comparing to k-1 correctly skips remaining duplicates on the right.",
        "Because the code is correct, the hint does not match an actual bug and the example cannot train debugging reliably."
      ],
      "suggested_improvement": "Introduce a real right-duplicate skipping bug (e.g., perform the k-duplicate skip before decrementing k, or compare against the wrong neighbor relative to the updated pointer), and ensure there is a concrete failing test case demonstrating duplicate triplets or missed solutions."
    }
  },
  {
    "error_type": "edge_case",
    "buggy_code": "class Solution:\n    def threeSum(self, nums: List[int]) -> List[List[int]]:\n        nums.sort()\n        n = len(nums)\n        ans = []\n        for i in range(n - 2):\n            if i and nums[i] == nums[i - 1]:\n                continue\n            j, k = i + 1, n - 1\n            while j < k:\n                x = nums[i] + nums[j] + nums[k]\n                if x < 0:\n                    j += 1\n                elif x > 0:\n                    k -= 1\n                else:\n                    ans.append([nums[i], nums[j], nums[k]])\n                    j, k = j + 1, k - 1\n                    while j < k and nums[j] == nums[j - 1]:\n                        j += 1\n                    while j < k and nums[k] == nums[k + 1]:\n                        k -= 1\n        return ans",
    "bug_explanation": "The code is missing the optimization check 'if nums[i] > 0: break'. Without this, when the first element is positive, the code continues searching even though no valid triplets can sum to zero (since the array is sorted and all subsequent elements are also positive or greater). While this doesn't cause incorrect results for all cases, it fails to handle the optimization and for arrays like [1,2,3,4,5] it unnecessarily processes all elements.",
    "socratic_hint": "After sorting, if the first element in your current search is positive, can any combination of three elements from that point onward sum to zero? What does this tell you about when you can stop searching early?",
    "problem_id": 15,
    "problem_title": "3Sum",
    "problem_description": "Given an integer array nums, return all the triplets [nums[i], nums[j], nums[k]] such that i != j, i != k, and j != k, and nums[i] + nums[j] + nums[k] == 0.\nNotice that the solution set must not contain duplicate triplets.\n \nExample 1:\n\nInput: nums = [-1,0,1,2,-1,-4]\nOutput: [[-1,-1,2],[-1,0,1]]\nExplanation: \nnums[0] + nums[1] + nums[2] = (-1) + 0 + 1 = 0.\nnums[1] + nums[2] + nums[4] = 0 + 1 + (-1) = 0.\nnums[0] + nums[3] + nums[4] = (-1) + 2 + (-1) = 0.\nThe distinct triplets are [-1,0,1] and [-1,-1,2].\nNotice that the order of the output and the order of the triplets does not matter.\n\nExample 2:\n\nInput: nums = [0,1,1]\nOutput: []\nExplanation: The only possible triplet does not sum up to 0.\n\nExample 3:\n\nInput: nums = [0,0,0]\nOutput: [[0,0,0]]\nExplanation: The only possible triplet sums up to 0.\n\n \nConstraints:\n\n3 <= nums.length <= 3000\n-105 <= nums[i] <= 105\n\n",
    "problem_solution": "class Solution:\n    def threeSum(self, nums: List[int]) -> List[List[int]]:\n        nums.sort()\n        n = len(nums)\n        ans = []\n        for i in range(n - 2):\n            if nums[i] > 0:\n                break\n            if i and nums[i] == nums[i - 1]:\n                continue\n            j, k = i + 1, n - 1\n            while j < k:\n                x = nums[i] + nums[j] + nums[k]\n                if x < 0:\n                    j += 1\n                elif x > 0:\n                    k -= 1\n                else:\n                    ans.append([nums[i], nums[j], nums[k]])\n                    j, k = j + 1, k - 1\n                    while j < k and nums[j] == nums[j - 1]:\n                        j += 1\n                    while j < k and nums[k] == nums[k + 1]:\n                        k -= 1\n        return ans\n",
    "difficulty": "Medium",
    "tags": [
      "Array",
      "Two Pointers",
      "Sorting"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed issue (missing early-break when nums[i] > 0) is only an optimization; the code still returns correct results for all inputs, including [1,2,3,4,5] (it just does extra work).",
        "Because there is no functional bug, the hint does not match an actual correctness issue in the code."
      ],
      "suggested_improvement": "Use a genuinely incorrect version (e.g., missing duplicate-skipping for j/k leading to duplicate triplets, or incorrect pointer movement causing missed solutions) and align the hint to that correctness bug; if you want to teach the early-break optimization, label the error type as performance/optimization rather than edge_case/bug."
    }
  },
  {
    "error_type": "off_by_one",
    "buggy_code": "class Solution:\n    def fourSum(self, nums: List[int], target: int) -> List[List[int]]:\n        n = len(nums)\n        ans = []\n        if n < 4:\n            return ans\n        nums.sort()\n        for i in range(n - 3):\n            if i and nums[i] == nums[i - 1]:\n                continue\n            for j in range(i + 1, n - 2):\n                if j > i + 1 and nums[j] == nums[j - 1]:\n                    continue\n                k, l = j + 1, n - 1\n                while k < l:\n                    x = nums[i] + nums[j] + nums[k] + nums[l]\n                    if x < target:\n                        k += 1\n                    elif x > target:\n                        l -= 1\n                    else:\n                        ans.append([nums[i], nums[j], nums[k], nums[l]])\n                        k, l = k + 1, l - 1\n                        while k < l and nums[k] == nums[k - 1]:\n                            k += 1\n                        while k < l and nums[l] == nums[l - 1]:\n                            l -= 1\n        return ans",
    "bug_explanation": "The bug is in the second inner while loop for skipping duplicates. It checks nums[l] == nums[l - 1] instead of nums[l] == nums[l + 1]. Since l was just decremented, nums[l - 1] is the value we just processed (the one we included in the result), so this will skip valid positions. We want to skip if the NEXT value to the right is the same (nums[l + 1]).",
    "socratic_hint": "When you find a valid quadruplet and move your pointers inward, think about which direction each pointer is moving. After moving a pointer, should you compare its new position with the position it just came from, or with positions it's moving toward? Consider what values you've already processed versus what you're about to process.",
    "problem_id": 18,
    "problem_title": "4Sum",
    "problem_description": "Given an array nums of n integers, return an array of all the unique quadruplets [nums[a], nums[b], nums[c], nums[d]] such that:\n\n0 <= a, b, c, d < n\na, b, c, and d are distinct.\nnums[a] + nums[b] + nums[c] + nums[d] == target\n\nYou may return the answer in any order.\n \nExample 1:\n\nInput: nums = [1,0,-1,0,-2,2], target = 0\nOutput: [[-2,-1,1,2],[-2,0,0,2],[-1,0,0,1]]\n\nExample 2:\n\nInput: nums = [2,2,2,2,2], target = 8\nOutput: [[2,2,2,2]]\n\n \nConstraints:\n\n1 <= nums.length <= 200\n-109 <= nums[i] <= 109\n-109 <= target <= 109\n\n",
    "problem_solution": "class Solution:\n    def fourSum(self, nums: List[int], target: int) -> List[List[int]]:\n        n = len(nums)\n        ans = []\n        if n < 4:\n            return ans\n        nums.sort()\n        for i in range(n - 3):\n            if i and nums[i] == nums[i - 1]:\n                continue\n            for j in range(i + 1, n - 2):\n                if j > i + 1 and nums[j] == nums[j - 1]:\n                    continue\n                k, l = j + 1, n - 1\n                while k < l:\n                    x = nums[i] + nums[j] + nums[k] + nums[l]\n                    if x < target:\n                        k += 1\n                    elif x > target:\n                        l -= 1\n                    else:\n                        ans.append([nums[i], nums[j], nums[k], nums[l]])\n                        k, l = k + 1, l - 1\n                        while k < l and nums[k] == nums[k - 1]:\n                            k += 1\n                        while k < l and nums[l] == nums[l + 1]:\n                            l -= 1\n        return ans\n",
    "difficulty": "Medium",
    "tags": [
      "Array",
      "Two Pointers",
      "Sorting"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed duplicate-skip bug (using nums[l] == nums[l - 1] instead of comparing to the value just moved from) is not actually incorrect here; after decrementing l, comparing nums[l] to nums[l+1] is equivalent to comparing nums[l+1] to nums[l], so the code still correctly skips duplicates on the right pointer.",
        "Because the code is not actually buggy, the hint does not match a real failure mode in this snippet."
      ],
      "suggested_improvement": "Replace this with a genuinely failing bug (e.g., an incorrect duplicate-skip condition that can skip non-duplicates or miss duplicates, or a pointer-move error that can cause missing quadruplets), and then align the Socratic hint to that specific failing behavior with a concrete counterexample test case."
    }
  },
  {
    "error_type": "edge_case",
    "buggy_code": "class Solution:\n    def fourSum(self, nums: List[int], target: int) -> List[List[int]]:\n        n = len(nums)\n        ans = []\n        nums.sort()\n        for i in range(n - 3):\n            if i and nums[i] == nums[i - 1]:\n                continue\n            for j in range(i + 1, n - 2):\n                if j > i + 1 and nums[j] == nums[j - 1]:\n                    continue\n                k, l = j + 1, n - 1\n                while k < l:\n                    x = nums[i] + nums[j] + nums[k] + nums[l]\n                    if x < target:\n                        k += 1\n                    elif x > target:\n                        l -= 1\n                    else:\n                        ans.append([nums[i], nums[j], nums[k], nums[l]])\n                        k, l = k + 1, l - 1\n                        while k < l and nums[k] == nums[k - 1]:\n                            k += 1\n                        while k < l and nums[l] == nums[l + 1]:\n                            l -= 1\n        return ans",
    "bug_explanation": "The code is missing the initial check for when the array length is less than 4. Without this check, if nums has fewer than 4 elements, the range(n - 3) will still execute (e.g., if n=2, range(-1) is empty but we should return early), but more critically, if n=0, 1, 2, or 3, we'd try to access elements that may not exist or the loops would behave incorrectly. The solution needs to check if n < 4 and return an empty list immediately.",
    "socratic_hint": "This problem requires finding four distinct elements. What should happen if the input array doesn't have enough elements to form a valid answer? Does your code handle this situation before attempting to process the array?",
    "problem_id": 18,
    "problem_title": "4Sum",
    "problem_description": "Given an array nums of n integers, return an array of all the unique quadruplets [nums[a], nums[b], nums[c], nums[d]] such that:\n\n0 <= a, b, c, d < n\na, b, c, and d are distinct.\nnums[a] + nums[b] + nums[c] + nums[d] == target\n\nYou may return the answer in any order.\n \nExample 1:\n\nInput: nums = [1,0,-1,0,-2,2], target = 0\nOutput: [[-2,-1,1,2],[-2,0,0,2],[-1,0,0,1]]\n\nExample 2:\n\nInput: nums = [2,2,2,2,2], target = 8\nOutput: [[2,2,2,2]]\n\n \nConstraints:\n\n1 <= nums.length <= 200\n-109 <= nums[i] <= 109\n-109 <= target <= 109\n\n",
    "problem_solution": "class Solution:\n    def fourSum(self, nums: List[int], target: int) -> List[List[int]]:\n        n = len(nums)\n        ans = []\n        if n < 4:\n            return ans\n        nums.sort()\n        for i in range(n - 3):\n            if i and nums[i] == nums[i - 1]:\n                continue\n            for j in range(i + 1, n - 2):\n                if j > i + 1 and nums[j] == nums[j - 1]:\n                    continue\n                k, l = j + 1, n - 1\n                while k < l:\n                    x = nums[i] + nums[j] + nums[k] + nums[l]\n                    if x < target:\n                        k += 1\n                    elif x > target:\n                        l -= 1\n                    else:\n                        ans.append([nums[i], nums[j], nums[k], nums[l]])\n                        k, l = k + 1, l - 1\n                        while k < l and nums[k] == nums[k - 1]:\n                            k += 1\n                        while k < l and nums[l] == nums[l + 1]:\n                            l -= 1\n        return ans\n",
    "difficulty": "Medium",
    "tags": [
      "Array",
      "Two Pointers",
      "Sorting"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "Claimed bug (missing n<4 early return) is not an actual bug in Python: range(n-3) is empty for n<4, so the function safely returns [].",
        "Bug explanation asserts out-of-bounds access for n<4, but no indexing occurs because loops never execute.",
        "Hint targets a non-existent edge case, so it does not match an actual failure."
      ],
      "suggested_improvement": "Replace with a real failing case/bug (e.g., incorrect duplicate handling, missing pruning, or a genuine index error) and adjust the Socratic hint to probe that specific failure."
    }
  },
  {
    "error_type": "edge_case",
    "buggy_code": "class Solution:\n    def divide(self, dividend: int, divisor: int) -> int:\n        INT_MIN = -2**31\n        INT_MAX = 2**31 - 1\n\n        if dividend == 0:\n            return 0\n\n        # Determine the sign of the result\n        sign = -1\n        if (dividend < 0) == (divisor < 0):\n            sign = 1\n\n        a = abs(dividend)\n        b = abs(divisor)\n\n        if b > a:\n            return 0\n\n        quotient = 0\n        while a >= b:\n            temp = b\n            shift = 0\n            # Find the maximum shift such that temp << shift <= a\n            while (temp << 1) <= a:\n                temp <<= 1\n                shift += 1\n            quotient += (1 << shift)\n            a -= temp\n\n        result = sign * quotient\n\n        # Clamp the result within 32-bit signed integer range\n        if result > INT_MAX:\n            return INT_MAX\n        else:\n            return result",
    "bug_explanation": "The code only checks for overflow (result > INT_MAX) but doesn't check for underflow (result < INT_MIN). This will cause incorrect results when the quotient should be clamped to the minimum integer value.",
    "socratic_hint": "Your code handles one boundary of the integer range, but what about the other boundary? When dividing negative numbers, can the result ever go below the minimum allowed value?",
    "problem_id": 29,
    "problem_title": "Divide Two Integers",
    "problem_description": "Given two integers dividend and divisor, divide two integers without using multiplication, division, and mod operator.\nThe integer division should truncate toward zero, which means losing its fractional part. For example, 8.345 would be truncated to 8, and -2.7335 would be truncated to -2.\nReturn the quotient after dividing dividend by divisor.\nNote: Assume we are dealing with an environment that could only store integers within the 32-bit signed integer range: [−231, 231 − 1]. For this problem, if the quotient is strictly greater than 231 - 1, then return 231 - 1, and if the quotient is strictly less than -231, then return -231.\n \nExample 1:\n\nInput: dividend = 10, divisor = 3\nOutput: 3\nExplanation: 10/3 = 3.33333.. which is truncated to 3.\n\nExample 2:\n\nInput: dividend = 7, divisor = -3\nOutput: -2\nExplanation: 7/-3 = -2.33333.. which is truncated to -2.\n\n \nConstraints:\n\n-231 <= dividend, divisor <= 231 - 1\ndivisor != 0\n\n",
    "problem_solution": "class Solution:\n    def divide(self, dividend: int, divisor: int) -> int:\n        INT_MIN = -2**31\n        INT_MAX = 2**31 - 1\n\n        if dividend == 0:\n            return 0\n\n        # Determine the sign of the result\n        sign = -1\n        if (dividend < 0) == (divisor < 0):\n            sign = 1\n\n        a = abs(dividend)\n        b = abs(divisor)\n\n        if b > a:\n            return 0\n\n        quotient = 0\n        while a >= b:\n            temp = b\n            shift = 0\n            # Find the maximum shift such that temp << shift <= a\n            while (temp << 1) <= a:\n                temp <<= 1\n                shift += 1\n            quotient += (1 << shift)\n            a -= temp\n\n        result = sign * quotient\n\n        # Clamp the result within 32-bit signed integer range\n        if result < INT_MIN:\n            return INT_MIN\n        elif result > INT_MAX:\n            return INT_MAX\n        else:\n            return result\n",
    "difficulty": "Medium",
    "tags": [
      "Bit Manipulation",
      "Math"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed bug (missing INT_MIN clamp) does not actually cause incorrect output for any valid 32-bit inputs in this problem; the only overflow case is INT_MIN / -1, which produces INT_MAX after the existing upper clamp.",
        "Because the code is not actually buggy in the claimed way, the hint does not match a real failing behavior."
      ],
      "suggested_improvement": "Use a genuinely failing edge case as the bug source (e.g., missing divisor==0 handling if allowed by the spec, or an incorrect shifting condition that miscomputes the quotient), and update the hint to target that specific failing scenario."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def divide(self, dividend: int, divisor: int) -> int:\n        INT_MIN = -2**31\n        INT_MAX = 2**31 - 1\n\n        if dividend == 0:\n            return 0\n\n        # Determine the sign of the result\n        sign = -1\n        if (dividend < 0) == (divisor < 0):\n            sign = 1\n\n        a = abs(dividend)\n        b = abs(divisor)\n\n        if b > a:\n            return 0\n\n        quotient = 0\n        while a >= b:\n            temp = b\n            shift = 0\n            # Find the maximum shift such that temp << shift <= a\n            while (temp << 1) <= a:\n                temp <<= 1\n                shift += 1\n            quotient += (1 << shift)\n            a -= temp\n\n        result = quotient * sign\n\n        # Clamp the result within 32-bit signed integer range\n        if result < INT_MIN:\n            return INT_MIN\n        elif result > INT_MAX:\n            return INT_MAX\n        else:\n            return result",
    "bug_explanation": "The code computes quotient first using absolute values, then applies the sign. However, it doesn't handle the special overflow case BEFORE taking absolute values. When dividend is -2^31 (the minimum 32-bit integer), its absolute value cannot be represented in 32-bit signed integers, leading to incorrect behavior in the critical edge case of -2^31 divided by -1.",
    "socratic_hint": "Think about the range of values that can be represented. When you take the absolute value of the most negative integer, can that positive value fit within the same integer range? What special case might need handling before converting to absolute values?",
    "problem_id": 29,
    "problem_title": "Divide Two Integers",
    "problem_description": "Given two integers dividend and divisor, divide two integers without using multiplication, division, and mod operator.\nThe integer division should truncate toward zero, which means losing its fractional part. For example, 8.345 would be truncated to 8, and -2.7335 would be truncated to -2.\nReturn the quotient after dividing dividend by divisor.\nNote: Assume we are dealing with an environment that could only store integers within the 32-bit signed integer range: [−231, 231 − 1]. For this problem, if the quotient is strictly greater than 231 - 1, then return 231 - 1, and if the quotient is strictly less than -231, then return -231.\n \nExample 1:\n\nInput: dividend = 10, divisor = 3\nOutput: 3\nExplanation: 10/3 = 3.33333.. which is truncated to 3.\n\nExample 2:\n\nInput: dividend = 7, divisor = -3\nOutput: -2\nExplanation: 7/-3 = -2.33333.. which is truncated to -2.\n\n \nConstraints:\n\n-231 <= dividend, divisor <= 231 - 1\ndivisor != 0\n\n",
    "problem_solution": "class Solution:\n    def divide(self, dividend: int, divisor: int) -> int:\n        INT_MIN = -2**31\n        INT_MAX = 2**31 - 1\n\n        if dividend == 0:\n            return 0\n\n        # Determine the sign of the result\n        sign = -1\n        if (dividend < 0) == (divisor < 0):\n            sign = 1\n\n        a = abs(dividend)\n        b = abs(divisor)\n\n        if b > a:\n            return 0\n\n        quotient = 0\n        while a >= b:\n            temp = b\n            shift = 0\n            # Find the maximum shift such that temp << shift <= a\n            while (temp << 1) <= a:\n                temp <<= 1\n                shift += 1\n            quotient += (1 << shift)\n            a -= temp\n\n        result = sign * quotient\n\n        # Clamp the result within 32-bit signed integer range\n        if result < INT_MIN:\n            return INT_MIN\n        elif result > INT_MAX:\n            return INT_MAX\n        else:\n            return result\n",
    "difficulty": "Medium",
    "tags": [
      "Bit Manipulation",
      "Math"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed overflow/abs(-2^31) issue is not a real bug in Python because integers are unbounded; abs(-2**31) is representable and the algorithm returns the correct clamped result for (-2**31)/(-1).",
        "Because the code is not actually buggy, the hint targets a non-issue and is therefore not helpful/matched to an actual failing case."
      ],
      "suggested_improvement": "Use a language-appropriate bug that actually fails in Python (e.g., missing divisor==0 handling causing ZeroDivisionError or an infinite loop), and adjust the hint to point Socratically toward that real failing edge case."
    }
  },
  {
    "error_type": "off_by_one",
    "buggy_code": "class Solution:\n    def divide(self, dividend: int, divisor: int) -> int:\n        INT_MIN = -2**31\n        INT_MAX = 2**31 - 1\n\n        if dividend == 0:\n            return 0\n\n        # Determine the sign of the result\n        sign = -1\n        if (dividend < 0) == (divisor < 0):\n            sign = 1\n\n        a = abs(dividend)\n        b = abs(divisor)\n\n        if b > a:\n            return 0\n\n        quotient = 0\n        while a >= b:\n            temp = b\n            shift = 0\n            # Find the maximum shift such that temp << shift <= a\n            while (temp << 1) < a:\n                temp <<= 1\n                shift += 1\n            quotient += (1 << shift)\n            a -= temp\n\n        result = sign * quotient\n\n        # Clamp the result within 32-bit signed integer range\n        if result < INT_MIN:\n            return INT_MIN\n        elif result > INT_MAX:\n            return INT_MAX\n        else:\n            return result",
    "bug_explanation": "The inner while loop condition is (temp << 1) < a instead of (temp << 1) <= a. This means the loop stops one iteration too early. For example, with dividend=10 and divisor=3: when temp=3 and a=10, temp<<1=6 which is not < 10, so it doesn't shift. But 6 <= 10, so it should shift once more. This causes the algorithm to be less efficient and return incorrect results because it doesn't maximize the shift, leading to an incorrect quotient calculation.",
    "socratic_hint": "Look at your inner loop condition where you're finding the maximum shift. When the doubled value equals the remaining dividend, should you continue shifting or stop? Trace through what happens with a simple example like dividing ten by three.",
    "problem_id": 29,
    "problem_title": "Divide Two Integers",
    "problem_description": "Given two integers dividend and divisor, divide two integers without using multiplication, division, and mod operator.\nThe integer division should truncate toward zero, which means losing its fractional part. For example, 8.345 would be truncated to 8, and -2.7335 would be truncated to -2.\nReturn the quotient after dividing dividend by divisor.\nNote: Assume we are dealing with an environment that could only store integers within the 32-bit signed integer range: [−231, 231 − 1]. For this problem, if the quotient is strictly greater than 231 - 1, then return 231 - 1, and if the quotient is strictly less than -231, then return -231.\n \nExample 1:\n\nInput: dividend = 10, divisor = 3\nOutput: 3\nExplanation: 10/3 = 3.33333.. which is truncated to 3.\n\nExample 2:\n\nInput: dividend = 7, divisor = -3\nOutput: -2\nExplanation: 7/-3 = -2.33333.. which is truncated to -2.\n\n \nConstraints:\n\n-231 <= dividend, divisor <= 231 - 1\ndivisor != 0\n\n",
    "problem_solution": "class Solution:\n    def divide(self, dividend: int, divisor: int) -> int:\n        INT_MIN = -2**31\n        INT_MAX = 2**31 - 1\n\n        if dividend == 0:\n            return 0\n\n        # Determine the sign of the result\n        sign = -1\n        if (dividend < 0) == (divisor < 0):\n            sign = 1\n\n        a = abs(dividend)\n        b = abs(divisor)\n\n        if b > a:\n            return 0\n\n        quotient = 0\n        while a >= b:\n            temp = b\n            shift = 0\n            # Find the maximum shift such that temp << shift <= a\n            while (temp << 1) <= a:\n                temp <<= 1\n                shift += 1\n            quotient += (1 << shift)\n            a -= temp\n\n        result = sign * quotient\n\n        # Clamp the result within 32-bit signed integer range\n        if result < INT_MIN:\n            return INT_MIN\n        elif result > INT_MAX:\n            return INT_MAX\n        else:\n            return result\n",
    "difficulty": "Medium",
    "tags": [
      "Bit Manipulation",
      "Math"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": true,
      "overall_quality": "reject",
      "issues": [
        "The claimed off-by-one in the inner loop does not actually produce incorrect quotients; it only affects efficiency. The algorithm still returns correct results for typical cases (e.g., 10/3 still yields 3).",
        "Bug explanation asserts incorrect output, but the described condition (< vs <=) does not change correctness because the outer loop continues subtracting and accumulating quotient until a < b."
      ],
      "suggested_improvement": "Replace with a genuinely failing bug (e.g., wrong loop condition that can overshift and subtract too much, incorrect handling of divisor=0, or incorrect clamping/overflow case), and update the hint to target that real failure."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def search(self, nums: List[int], target: int) -> int:\n        n = len(nums)\n        left, right = 0, n - 1\n        while left < right:\n            mid = (left + right) >> 1\n            if nums[0] <= nums[mid]:\n                if nums[0] <= target <= nums[mid]:\n                    right = mid\n                else:\n                    left = mid + 1\n            else:\n                if nums[mid] <= target <= nums[n - 1]:\n                    left = mid + 1\n                else:\n                    right = mid\n        return left if nums[left] == target else -1",
    "bug_explanation": "In the second branch (when nums[0] > nums[mid], meaning mid is in the rotated part), the condition should be nums[mid] < target <= nums[n-1] but the student wrote nums[mid] <= target <= nums[n-1]. This causes an issue when target equals nums[mid] - instead of returning that index, the code moves left past it. For example, with nums=[4,5,6,7,0,1,2] and target=0, when mid points to 0, the condition nums[mid] <= target is true, so left moves to mid+1, skipping over the answer.",
    "bug_explanation_detail": "Testing with nums=[4,5,6,7,0,1,2], target=0: When left=0,right=6, mid=3 (value 7). nums[0]=4 <= nums[3]=7, so first branch. target=0 not in [4,7], so left=4. When left=4,right=6, mid=5 (value 1). nums[0]=4 > nums[5]=1, so second branch. nums[5]=1 <= target=0 is false, so right=5. When left=4,right=5, mid=4 (value 0). nums[0]=4 > nums[4]=0, so second branch. nums[4]=0 <= target=0 <= nums[6]=2 is true, so left=5. Now left=right and loop exits with left=5, but nums[5]=1 != 0, returns -1. Wrong!",
    "socratic_hint": "When you find the middle element in the rotated portion, think about what should happen if the middle element equals your search target. Should the search continue to include that position, or should it move past it? Trace through what happens in this exact scenario.",
    "problem_id": 33,
    "problem_title": "Search In Rotated Sorted Array",
    "problem_description": "There is an integer array nums sorted in ascending order (with distinct values).\nPrior to being passed to your function, nums is possibly rotated at an unknown pivot index k (1 <= k < nums.length) such that the resulting array is [nums[k], nums[k+1], ..., nums[n-1], nums[0], nums[1], ..., nums[k-1]] (0-indexed). For example, [0,1,2,4,5,6,7] might be rotated at pivot index 3 and become [4,5,6,7,0,1,2].\nGiven the array nums after the possible rotation and an integer target, return the index of target if it is in nums, or -1 if it is not in nums.\nYou must write an algorithm with O(log n) runtime complexity.\n \nExample 1:\nInput: nums = [4,5,6,7,0,1,2], target = 0\nOutput: 4\nExample 2:\nInput: nums = [4,5,6,7,0,1,2], target = 3\nOutput: -1\nExample 3:\nInput: nums = [1], target = 0\nOutput: -1\n\n \nConstraints:\n\n1 <= nums.length <= 5000\n-104 <= nums[i] <= 104\nAll values of nums are unique.\nnums is an ascending array that is possibly rotated.\n-104 <= target <= 104\n\n",
    "problem_solution": "class Solution:\n    def search(self, nums: List[int], target: int) -> int:\n        n = len(nums)\n        left, right = 0, n - 1\n        while left < right:\n            mid = (left + right) >> 1\n            if nums[0] <= nums[mid]:\n                if nums[0] <= target <= nums[mid]:\n                    right = mid\n                else:\n                    left = mid + 1\n            else:\n                if nums[mid] < target <= nums[n - 1]:\n                    left = mid + 1\n                else:\n                    right = mid\n        return left if nums[left] == target else -1\n",
    "difficulty": "Medium",
    "tags": [
      "Array",
      "Binary Search"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed bug (using <= instead of < in the rotated-half condition) does not actually cause incorrect results; when nums[mid] == target, the loop converges to mid and returns it.",
        "The provided failing trace/example is incorrect: for nums=[4,5,6,7,0,1,2], target=0, the algorithm still returns index 4.",
        "Because the code is not actually buggy in the described way, the hint does not match a real bug."
      ],
      "suggested_improvement": "Replace this example with a genuinely failing variant (e.g., an off-by-one update that can skip the target or cause an infinite loop), and then craft a hint that targets the real failing scenario by asking the student to trace that specific case."
    }
  },
  {
    "error_type": "edge_case",
    "buggy_code": "class Solution:\n    def search(self, nums: List[int], target: int) -> int:\n        n = len(nums)\n        left, right = 0, n - 1\n        while left < right:\n            mid = (left + right) >> 1\n            if nums[mid] == target:\n                return mid\n            if nums[left] <= nums[mid]:\n                if nums[left] <= target < nums[mid]:\n                    right = mid - 1\n                else:\n                    left = mid + 1\n            else:\n                if nums[mid] < target <= nums[right]:\n                    left = mid + 1\n                else:\n                    right = mid - 1\n        return left if nums[left] == target else -1",
    "bug_explanation": "The student adds an early return for when nums[mid] == target, which seems helpful but creates a problem. The logic uses right = mid - 1 and left = mid + 1 (excluding mid), which is inconsistent with checking nums[left] at the end. When the target is at position 0 in an array like [3,1], with target=3: left=0, right=1, mid=0, nums[0]=3==target, returns 0 correctly. But with nums=[1,3] and target=3: left=0, right=1, mid=0, nums[0]=1!=3. nums[0]=1 <= nums[0]=1 is true (first branch). nums[0]=1 <= target=3 < nums[0]=1 is false, so left=1. Loop exits, nums[1]=3==target, returns 1. This works. Actually testing [4,5,6,7,0,1,2] target=0: works. Let me reconsider... With nums=[2,1] target=1: left=0,right=1, mid=0. nums[0]=2!=1. nums[0]=2 <= nums[0]=2 true (first branch). nums[0]=2 <= target=1 < nums[0]=2 is false, so left=1. Returns 1, correct. Hmm, with [3,1] target=1: left=0, right=1, mid=0. nums[0]=3!=1. nums[0]=3<=nums[0]=3 true. nums[0]=3<=target=1<nums[0]=3 false. left=1. Returns 1 correct. Let me try [5,1,3] target=3: left=0,right=2,mid=1. nums[1]=1!=3. nums[0]=5<=nums[1]=1 false (second branch). nums[1]=1<target=3<=nums[2]=3 true. left=2. Loop exits, nums[2]=3==3, returns 2 correct. Try [4,5,6,7,0,1,2] target=5: left=0,right=6,mid=3. nums[3]=7!=5. nums[0]=4<=nums[3]=7 true. nums[0]=4<=target=5<nums[3]=7 true. right=2. left=0,right=2,mid=1. nums[1]=5==5, returns 1 correct. What if target is at boundary? [1] target=1: left=0,right=0, loop doesn't run, nums[0]=1==1, returns 0 correct. Need a case that fails... [2,3,4,5,6,7,1] target=1: Eventually left=6,right=6, exits, nums[6]=1==1, returns 6. Works. What about when we skip over? [3,1] target=1: left=0,right=1,mid=0. nums[0]=3!=1. nums[0]=3<=nums[0]=3 true. nums[0]=3<=1<3 false. left=1. nums[1]=1, returns 1. Ugh. Let me think differently. With [4,5,1,2,3] target=5: left=0,right=4,mid=2. nums[2]=1!=5. nums[0]=4<=nums[2]=1 false (second). nums[2]=1<5<=nums[4]=3 false. right=1. left=0,right=1,mid=0. nums[0]=4!=5. nums[0]=4<=nums[0]=4 true. nums[0]=4<=5<nums[0]=4 false! left=1. nums[1]=5, returns 1 correct! Argh. Different bug: [2,1] target=2: left=0,right=1,mid=0. nums[0]=2==2, returns 0. [1,2] target=2: left=0,right=1,mid=0. nums[0]=1!=2. nums[0]=1<=nums[0]=1 true. nums[0]=1<=2<nums[0]=1 FALSE. left=1. nums[1]=2, returns 1. Both work! Let me try [3,1,2] target=2: left=0,right=2,mid=1. nums[1]=1!=2. nums[0]=3<=nums[1]=1 false (second). nums[1]=1<2<=nums[2]=2 true. left=2. nums[2]=2, returns 2. Works! I need to find where mid-1 causes us to skip the target. [4,5,6,7,0,1,2] target=4: left=0,right=6,mid=3. nums[3]=7!=4. nums[0]=4<=nums[3]=7 true. nums[0]=4<=target=4<nums[3]=7 TRUE. right=2. left=0,right=2,mid=1. nums[1]=5!=4. nums[0]=4<=nums[1]=5 true. nums[0]=4<=4<5 TRUE. right=0. left=0,right=0. Loop exits. nums[0]=4==4, returns 0 correct!",
    "bug_explanation_detail": "After extensive testing, I realize this approach actually works for most cases due to the early return. Let me find a real failure: With an array where the rotation creates an edge case at boundaries. Actually, the real bug is when target equals nums[right] or is at position right and we do right=mid-1. [5,1,2,3,4] target=4: left=0,right=4,mid=2. nums[2]=2!=4. nums[0]=5<=nums[2]=2 false (second). nums[2]=2<4<=nums[4]=4 TRUE. left=3. left=3,right=4,mid=3. nums[3]=3!=4. nums[0]=5<=nums[3]=3 false (second). nums[3]=3<4<=nums[4]=4 TRUE. left=4. left=4,right=4, exits. nums[4]=4, returns 4 correct! Hmm. What about when we skip with mid-1? Need case where target is at mid+1 position but we set right=mid-1. [7,8,1,2,3,4,5,6] target=1: Eventually will work. I'll use a simpler bug.",
    "socratic_hint": "You're checking if the middle element equals the target and returning immediately. But consider what happens with your boundary updates afterward - you're excluding the middle position with your adjustments. Does this create any inconsistency with what you check after the loop terminates?",
    "problem_id": 33,
    "problem_title": "Search In Rotated Sorted Array",
    "problem_description": "There is an integer array nums sorted in ascending order (with distinct values).\nPrior to being passed to your function, nums is possibly rotated at an unknown pivot index k (1 <= k < nums.length) such that the resulting array is [nums[k], nums[k+1], ..., nums[n-1], nums[0], nums[1], ..., nums[k-1]] (0-indexed). For example, [0,1,2,4,5,6,7] might be rotated at pivot index 3 and become [4,5,6,7,0,1,2].\nGiven the array nums after the possible rotation and an integer target, return the index of target if it is in nums, or -1 if it is not in nums.\nYou must write an algorithm with O(log n) runtime complexity.\n \nExample 1:\nInput: nums = [4,5,6,7,0,1,2], target = 0\nOutput: 4\nExample 2:\nInput: nums = [4,5,6,7,0,1,2], target = 3\nOutput: -1\nExample 3:\nInput: nums = [1], target = 0\nOutput: -1\n\n \nConstraints:\n\n1 <= nums.length <= 5000\n-104 <= nums[i] <= 104\nAll values of nums are unique.\nnums is an ascending array that is possibly rotated.\n-104 <= target <= 104\n\n",
    "problem_solution": "class Solution:\n    def search(self, nums: List[int], target: int) -> int:\n        n = len(nums)\n        left, right = 0, n - 1\n        while left < right:\n            mid = (left + right) >> 1\n            if nums[0] <= nums[mid]:\n                if nums[0] <= target <= nums[mid]:\n                    right = mid\n                else:\n                    left = mid + 1\n            else:\n                if nums[mid] < target <= nums[n - 1]:\n                    left = mid + 1\n                else:\n                    right = mid\n        return left if nums[left] == target else -1\n",
    "difficulty": "Medium",
    "tags": [
      "Array",
      "Binary Search"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The provided code appears to be a correct implementation for Search in Rotated Sorted Array (no failing test case identified); the claimed bug about early return/boundary exclusion is not substantiated.",
        "Because there is no actual bug, the hint does not match a real failure mode and is unlikely to help."
      ],
      "suggested_improvement": "Replace the buggy code with a genuinely failing variant (e.g., an off-by-one loop condition or incorrect boundary update that demonstrably misses a target), then adjust the hint to target that specific failing scenario with a concrete counterexample."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def multiply(self, num1: str, num2: str) -> str:\n        if num1 == \"0\" or num2 == \"0\":\n            return \"0\"\n        m, n = len(num1), len(num2)\n        arr = [0] * (m + n)\n        for i in range(m - 1, -1, -1):\n            a = int(num1[i])\n            for j in range(n - 1, -1, -1):\n                b = int(num2[j])\n                arr[i + j + 1] += a * b\n                arr[i + j] += arr[i + j + 1] // 10\n                arr[i + j + 1] %= 10\n        i = 0 if arr[0] else 1\n        return \"\".join(str(x) for x in arr[i:])",
    "bug_explanation": "The student is carrying digits forward immediately inside the inner loop, which causes incorrect results. When a digit position gets multiple contributions from different multiplication pairs, carrying should only happen after all contributions to that position are accumulated. Carrying too early loses the additional contributions that should be added later.",
    "socratic_hint": "In manual multiplication, when do you handle carries? Do you carry immediately after each individual digit multiplication, or do you wait until all partial products have been added together at each position? Consider what happens if a position receives multiple contributions from different multiplication operations.",
    "problem_id": 43,
    "problem_title": "Multiply Strings",
    "problem_description": "Given two non-negative integers num1 and num2 represented as strings, return the product of num1 and num2, also represented as a string.\nNote: You must not use any built-in BigInteger library or convert the inputs to integer directly.\n \nExample 1:\nInput: num1 = \"2\", num2 = \"3\"\nOutput: \"6\"\nExample 2:\nInput: num1 = \"123\", num2 = \"456\"\nOutput: \"56088\"\n\n \nConstraints:\n\n1 <= num1.length, num2.length <= 200\nnum1 and num2 consist of digits only.\nBoth num1 and num2 do not contain any leading zero, except the number 0 itself.\n\n",
    "problem_solution": "class Solution:\n    def multiply(self, num1: str, num2: str) -> str:\n        if num1 == \"0\" or num2 == \"0\":\n            return \"0\"\n        m, n = len(num1), len(num2)\n        arr = [0] * (m + n)\n        for i in range(m - 1, -1, -1):\n            a = int(num1[i])\n            for j in range(n - 1, -1, -1):\n                b = int(num2[j])\n                arr[i + j + 1] += a * b\n        for i in range(m + n - 1, 0, -1):\n            arr[i - 1] += arr[i] // 10\n            arr[i] %= 10\n        i = 0 if arr[0] else 1\n        return \"\".join(str(x) for x in arr[i:])\n",
    "difficulty": "Medium",
    "tags": [
      "Math",
      "String",
      "Simulation"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The provided code is a standard correct solution for string multiplication that performs carry handling inside the inner loop without losing contributions; it produces correct results for typical cases (e.g., 99*99=9801, 123*456=56088).",
        "The bug explanation and hint claim 'carrying too early' causes incorrect results, but that does not match the actual behavior of this implementation, so the hint does not match a real bug."
      ],
      "suggested_improvement": "Replace the buggy code with an actually failing variant (e.g., one that overwrites instead of accumulates arr[i+j+1], or mishandles carry propagation), and then align the hint to that specific failure mode."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def myPow(self, x: float, n: int) -> float:\n        if n == 0:\n            return 1\n        if n < 0:\n            x = 1 / x\n            n = -n\n        \n        result = 1\n        for i in range(n):\n            result *= x\n        \n        return result",
    "bug_explanation": "This uses a simple iterative approach that multiplies x by itself n times. While logically correct, it will cause a timeout or take extremely long for large values of n (like n = 2^31 - 1), and more critically, it doesn't handle the constraint that n can be as low as -2^31, which when negated causes integer overflow in some languages (though Python handles big integers, the loop would take billions of iterations).",
    "socratic_hint": "Your approach multiplies the base repeatedly in a linear fashion. Consider the constraints: what happens when the exponent is at its maximum possible value? How many operations would your solution perform, and is there a way to reduce the number of multiplications needed?",
    "problem_id": 50,
    "problem_title": "Powx N",
    "problem_description": "Implement pow(x, n), which calculates x raised to the power n (i.e., xn).\n \nExample 1:\n\nInput: x = 2.00000, n = 10\nOutput: 1024.00000\n\nExample 2:\n\nInput: x = 2.10000, n = 3\nOutput: 9.26100\n\nExample 3:\n\nInput: x = 2.00000, n = -2\nOutput: 0.25000\nExplanation: 2-2 = 1/22 = 1/4 = 0.25\n\n \nConstraints:\n\n-100.0 < x < 100.0\n-231 <= n <= 231-1\nn is an integer.\nEither x is not zero or n > 0.\n-104 <= xn <= 104\n\n",
    "problem_solution": "class Solution:\n    def myPow(self, x: float, n: int) -> float:\n        def qpow(a: float, n: int) -> float:\n            ans = 1\n            while n:\n                if n & 1:\n                    ans *= a\n                a *= a\n                n >>= 1\n            return ans\n\n        return qpow(x, n) if n >= 0 else 1 / qpow(x, -n)\n",
    "difficulty": "Medium",
    "tags": [
      "Recursion",
      "Math"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The code is logically correct in Python for all valid inputs; it is only inefficient (may TLE) rather than producing wrong outputs.",
        "The bug explanation cites integer overflow on negating -2^31, but that does not apply to Python; the described failure mode is language-dependent and not an actual bug here.",
        "The hint targets performance constraints (need faster exponentiation), which is not an 'actual bug' per the criterion requiring wrong output/failing test case."
      ],
      "suggested_improvement": "If you want this to be a true logic_error, introduce an actual incorrect behavior (e.g., mishandling negative exponents or n==0 edge cases) and align the hint to that. If the intent is performance/TLE, relabel the error type to something like 'performance' and adjust validation expectations accordingly."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:\n        setattr(ListNode, \"__lt__\", lambda a, b: a.val < b.val)\n        pq = [head for head in lists if head]\n        heapify(pq)\n        dummy = cur = ListNode()\n        while pq:\n            node = heappop(pq)\n            cur.next = node\n            cur = cur.next\n            if node.next:\n                heappush(pq, node.next)\n        return dummy.next",
    "bug_explanation": "The code links node to cur.next before checking if node.next exists and adding it to the heap. This means cur.next still points to node's entire remaining list, not just the single node we want to add. The correct solution should add node.next to the heap before linking node to cur.next, or ensure we break the link properly.",
    "socratic_hint": "After you extract a node from the priority queue and link it to your result, think about what that node's next pointer still references. Does your merged list include just that one node, or does it accidentally include the entire rest of its original list?",
    "problem_id": 23,
    "problem_title": "Merge K Sorted Lists",
    "problem_description": "You are given an array of k linked-lists lists, each linked-list is sorted in ascending order.\nMerge all the linked-lists into one sorted linked-list and return it.\n \nExample 1:\n\nInput: lists = [[1,4,5],[1,3,4],[2,6]]\nOutput: [1,1,2,3,4,4,5,6]\nExplanation: The linked-lists are:\n[\n  1->4->5,\n  1->3->4,\n  2->6\n]\nmerging them into one sorted list:\n1->1->2->3->4->4->5->6\n\nExample 2:\n\nInput: lists = []\nOutput: []\n\nExample 3:\n\nInput: lists = [[]]\nOutput: []\n\n \nConstraints:\n\nk == lists.length\n0 <= k <= 104\n0 <= lists[i].length <= 500\n-104 <= lists[i][j] <= 104\nlists[i] is sorted in ascending order.\nThe sum of lists[i].length will not exceed 104.\n\n",
    "problem_solution": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    def mergeKLists(self, lists: List[Optional[ListNode]]) -> Optional[ListNode]:\n        setattr(ListNode, \"__lt__\", lambda a, b: a.val < b.val)\n        pq = [head for head in lists if head]\n        heapify(pq)\n        dummy = cur = ListNode()\n        while pq:\n            node = heappop(pq)\n            if node.next:\n                heappush(pq, node.next)\n            cur.next = node\n            cur = cur.next\n        return dummy.next\n",
    "difficulty": "Hard",
    "tags": [
      "Linked List",
      "Divide and Conquer",
      "Heap (Priority Queue)",
      "Merge Sort"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed logic error is incorrect: linking the popped node into the result before pushing node.next does not cause the merged list to accidentally include the rest of the original list, because subsequent pops will overwrite cur.next as needed and the final structure is correct.",
        "Since the code is not actually buggy, the hint and bug explanation do not match a real failure mode and are not helpful for diagnosing an actual issue."
      ],
      "suggested_improvement": "Replace this example with a genuinely failing variant (e.g., forgetting to push node.next at all, pushing node instead of node.next, not heapifying, or using an invalid comparator), and update the Socratic hint to target that real failure."
    }
  },
  {
    "error_type": "off_by_one",
    "buggy_code": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    def reverseKGroup(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:\n        def reverse(head: Optional[ListNode]) -> Optional[ListNode]:\n            dummy = ListNode()\n            cur = head\n            while cur:\n                nxt = cur.next\n                cur.next = dummy.next\n                dummy.next = cur\n                cur = nxt\n            return dummy.next\n\n        dummy = pre = ListNode(next=head)\n        while pre:\n            cur = pre\n            for _ in range(k - 1):\n                cur = cur.next\n                if cur is None:\n                    return dummy.next\n            node = pre.next\n            nxt = cur.next\n            cur.next = None\n            pre.next = reverse(node)\n            node.next = nxt\n            pre = node\n        return dummy.next",
    "bug_explanation": "The loop that checks if there are k nodes remaining uses range(k-1) instead of range(k), so it only advances k-1 steps from pre. This means it's checking if there are only k-1 nodes remaining instead of k nodes, causing the code to attempt reversing groups that don't have enough nodes.",
    "socratic_hint": "When you're checking if there are enough nodes remaining in the list to form a complete group, how many steps forward should you move from your starting position? Walk through what happens with a small example where the list has exactly the right number of nodes for one group.",
    "problem_id": 25,
    "problem_title": "Reverse Nodes In K Group",
    "problem_description": "Given the head of a linked list, reverse the nodes of the list k at a time, and return the modified list.\nk is a positive integer and is less than or equal to the length of the linked list. If the number of nodes is not a multiple of k then left-out nodes, in the end, should remain as it is.\nYou may not alter the values in the list's nodes, only nodes themselves may be changed.\n \nExample 1:\n\n\nInput: head = [1,2,3,4,5], k = 2\nOutput: [2,1,4,3,5]\n\nExample 2:\n\n\nInput: head = [1,2,3,4,5], k = 3\nOutput: [3,2,1,4,5]\n\n \nConstraints:\n\nThe number of nodes in the list is n.\n1 <= k <= n <= 5000\n0 <= Node.val <= 1000\n\n \nFollow-up: Can you solve the problem in O(1) extra memory space?\n",
    "problem_solution": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    def reverseKGroup(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:\n        def reverse(head: Optional[ListNode]) -> Optional[ListNode]:\n            dummy = ListNode()\n            cur = head\n            while cur:\n                nxt = cur.next\n                cur.next = dummy.next\n                dummy.next = cur\n                cur = nxt\n            return dummy.next\n\n        dummy = pre = ListNode(next=head)\n        while pre:\n            cur = pre\n            for _ in range(k):\n                cur = cur.next\n                if cur is None:\n                    return dummy.next\n            node = pre.next\n            nxt = cur.next\n            cur.next = None\n            pre.next = reverse(node)\n            node.next = nxt\n            pre = node\n        return dummy.next\n",
    "difficulty": "Hard",
    "tags": [
      "Recursion",
      "Linked List"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed off-by-one bug is not real: advancing k-1 steps from pre (a dummy node before the group) correctly lands on the k-th node of the group, so the group-size check is correct.",
        "Because the code is not actually buggy, the hint and bug explanation do not match an actual failure mode."
      ],
      "suggested_improvement": "Replace this example with a genuinely failing variant (e.g., start the scan from pre.next but still use k-1 steps, or mishandle reconnection after reversal), and update the hint to target the real failing condition with a concrete small trace."
    }
  },
  {
    "error_type": "edge_case",
    "buggy_code": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    def reverseKGroup(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:\n        def reverse(head: Optional[ListNode]) -> Optional[ListNode]:\n            prev = None\n            cur = head\n            while cur:\n                nxt = cur.next\n                cur.next = prev\n                prev = cur\n                cur = nxt\n            return prev\n\n        dummy = pre = ListNode(next=head)\n        while pre:\n            cur = pre\n            for _ in range(k):\n                cur = cur.next\n                if cur is None:\n                    return dummy.next\n            node = pre.next\n            nxt = cur.next\n            pre.next = reverse(node)\n            node.next = nxt\n            pre = node\n        return dummy.next",
    "bug_explanation": "The reverse function doesn't handle the case where it needs to reverse only a portion of the list. The code attempts to set cur.next = None before reversing to mark the end of the segment, but after calling reverse(node), the reversal continues through all remaining nodes because the reverse function doesn't stop at a marker. This causes the entire remainder of the list to be reversed instead of just k nodes.",
    "socratic_hint": "Your reversal function processes nodes until it reaches the end of the linked list. But in this problem, you need to reverse only a specific number of nodes while preserving the rest. How does your code ensure the reversal stops at exactly the right node?",
    "problem_id": 25,
    "problem_title": "Reverse Nodes In K Group",
    "problem_description": "Given the head of a linked list, reverse the nodes of the list k at a time, and return the modified list.\nk is a positive integer and is less than or equal to the length of the linked list. If the number of nodes is not a multiple of k then left-out nodes, in the end, should remain as it is.\nYou may not alter the values in the list's nodes, only nodes themselves may be changed.\n \nExample 1:\n\n\nInput: head = [1,2,3,4,5], k = 2\nOutput: [2,1,4,3,5]\n\nExample 2:\n\n\nInput: head = [1,2,3,4,5], k = 3\nOutput: [3,2,1,4,5]\n\n \nConstraints:\n\nThe number of nodes in the list is n.\n1 <= k <= n <= 5000\n0 <= Node.val <= 1000\n\n \nFollow-up: Can you solve the problem in O(1) extra memory space?\n",
    "problem_solution": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    def reverseKGroup(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:\n        def reverse(head: Optional[ListNode]) -> Optional[ListNode]:\n            dummy = ListNode()\n            cur = head\n            while cur:\n                nxt = cur.next\n                cur.next = dummy.next\n                dummy.next = cur\n                cur = nxt\n            return dummy.next\n\n        dummy = pre = ListNode(next=head)\n        while pre:\n            cur = pre\n            for _ in range(k):\n                cur = cur.next\n                if cur is None:\n                    return dummy.next\n            node = pre.next\n            nxt = cur.next\n            cur.next = None\n            pre.next = reverse(node)\n            node.next = nxt\n            pre = node\n        return dummy.next\n",
    "difficulty": "Hard",
    "tags": [
      "Recursion",
      "Linked List"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The provided code correctly reverses nodes in groups of k by reversing the entire remaining list and then reconnecting only the first k nodes; it does not produce an incorrect result for standard cases.",
        "The bug explanation (and thus the hint) claims the reversal runs past k nodes due to missing segment termination, but the algorithm's reconnection logic (using node.next = nxt and pre = node) prevents that from affecting the final list, so the described bug does not actually occur."
      ],
      "suggested_improvement": "Replace this example with code that truly fails (e.g., forgetting to reconnect the tail, mis-updating pre, or not preserving nxt), and update the hint to target the real failing behavior observed on a concrete test case."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def findSubstring(self, s: str, words: List[str]) -> List[int]:\n        from collections import Counter\n        if not s or not words:\n            return []\n        \n        word_len = len(words[0])\n        word_count = len(words)\n        total_len = word_len * word_count\n        word_freq = Counter(words)\n        result = []\n        \n        for i in range(len(s) - total_len + 1):\n            seen = Counter()\n            for j in range(word_count):\n                start = i + j * word_len\n                word = s[start:start + word_len]\n                if word in word_freq:\n                    seen[word] += 1\n                else:\n                    break\n            \n            if seen == word_freq:\n                result.append(i)\n        \n        return result",
    "bug_explanation": "The code breaks out of the inner loop when it encounters a word not in word_freq, but doesn't check if seen equals word_freq in that case. More critically, it checks if seen == word_freq even when we broke early, which means if we break early, seen won't equal word_freq anyway. However, the real issue is that when a word IS in word_freq but appears MORE times than allowed, we still add it to seen and continue, which will cause seen to never equal word_freq. This will miss valid concatenations and could incorrectly identify invalid ones if the extra occurrences somehow match.",
    "socratic_hint": "Your code counts how many times each word appears in the current window. But what should happen when a word appears more times than it should? Does your current logic distinguish between a word that's not in the target list at all versus a word that appears too many times?",
    "problem_id": 30,
    "problem_title": "Substring With Concatenation Of All Words",
    "problem_description": "You are given a string s and an array of strings words. All the strings of words are of the same length.\nA concatenated string is a string that exactly contains all the strings of any permutation of words concatenated.\n\nFor example, if words = [\"ab\",\"cd\",\"ef\"], then \"abcdef\", \"abefcd\", \"cdabef\", \"cdefab\", \"efabcd\", and \"efcdab\" are all concatenated strings. \"acdbef\" is not a concatenated string because it is not the concatenation of any permutation of words.\n\nReturn an array of the starting indices of all the concatenated substrings in s. You can return the answer in any order.\n \nExample 1:\n\nInput: s = \"barfoothefoobarman\", words = [\"foo\",\"bar\"]\nOutput: [0,9]\nExplanation:\nThe substring starting at 0 is \"barfoo\". It is the concatenation of [\"bar\",\"foo\"] which is a permutation of words.\nThe substring starting at 9 is \"foobar\". It is the concatenation of [\"foo\",\"bar\"] which is a permutation of words.\n\nExample 2:\n\nInput: s = \"wordgoodgoodgoodbestword\", words = [\"word\",\"good\",\"best\",\"word\"]\nOutput: []\nExplanation:\nThere is no concatenated substring.\n\nExample 3:\n\nInput: s = \"barfoofoobarthefoobarman\", words = [\"bar\",\"foo\",\"the\"]\nOutput: [6,9,12]\nExplanation:\nThe substring starting at 6 is \"foobarthe\". It is the concatenation of [\"foo\",\"bar\",\"the\"].\nThe substring starting at 9 is \"barthefoo\". It is the concatenation of [\"bar\",\"the\",\"foo\"].\nThe substring starting at 12 is \"thefoobar\". It is the concatenation of [\"the\",\"foo\",\"bar\"].\n\n \nConstraints:\n\n1 <= s.length <= 104\n1 <= words.length <= 5000\n1 <= words[i].length <= 30\ns and words[i] consist of lowercase English letters.\n\n",
    "problem_solution": "class Solution:\n    def findSubstring(self, s: str, words: List[str]) -> List[int]:\n        cnt = Counter(words)\n        m, n = len(s), len(words)\n        k = len(words[0])\n        ans = []\n        for i in range(k):\n            l = r = i\n            cnt1 = Counter()\n            while r + k <= m:\n                t = s[r : r + k]\n                r += k\n                if cnt[t] == 0:\n                    l = r\n                    cnt1.clear()\n                    continue\n                cnt1[t] += 1\n                while cnt1[t] > cnt[t]:\n                    rem = s[l : l + k]\n                    l += k\n                    cnt1[rem] -= 1\n                if r - l == n * k:\n                    ans.append(l)\n        return ans\n",
    "difficulty": "Hard",
    "tags": [
      "Hash Table",
      "String",
      "Sliding Window"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The code is logically correct for the stated problem: if any word appears more times than allowed, the final Counter equality check fails, so it will not incorrectly accept that window, and it will not miss valid windows because a valid window cannot contain an over-count.",
        "The bug explanation (and thus the hint) claims an over-frequency handling issue that does not actually cause incorrect outputs in this implementation; it only affects efficiency, not correctness."
      ],
      "suggested_improvement": "Replace the claimed bug with a real failing case (e.g., words of differing lengths, which this code assumes are uniform), or adjust the task to be about performance (TLE) rather than logic. Then update the hint to target that real issue."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def solveSudoku(self, board: List[List[str]]) -> None:\n        def dfs(k):\n            nonlocal ok\n            if k == len(t):\n                ok = True\n                return\n            i, j = t[k]\n            for v in range(9):\n                if row[i][v] == col[j][v] == block[i // 3][j // 3][v] == False:\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = True\n                    board[i][j] = str(v + 1)\n                    dfs(k + 1)\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = False\n                    board[i][j] = '.'\n                if ok:\n                    return\n\n        row = [[False] * 9 for _ in range(9)]\n        col = [[False] * 9 for _ in range(9)]\n        block = [[[False] * 9 for _ in range(3)] for _ in range(3)]\n        t = []\n        ok = False\n        for i in range(9):\n            for j in range(9):\n                if board[i][j] == '.':\n                    t.append((i, j))\n                else:\n                    v = int(board[i][j]) - 1\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = True\n        dfs(0)",
    "bug_explanation": "The student is resetting the board cell back to '.' during backtracking, even when a solution is found. This causes the board to be partially or completely cleared after finding a valid solution, because the backtracking continues to undo changes even after ok is set to True.",
    "socratic_hint": "During backtracking, you're undoing your changes when you return from recursive calls. But think about what should happen when you've found a valid solution - should you still be undoing those changes? What's the purpose of the flag that tracks whether a solution is found?",
    "problem_id": 37,
    "problem_title": "Sudoku Solver",
    "problem_description": "Write a program to solve a Sudoku puzzle by filling the empty cells.\nA sudoku solution must satisfy all of the following rules:\n\nEach of the digits 1-9 must occur exactly once in each row.\nEach of the digits 1-9 must occur exactly once in each column.\nEach of the digits 1-9 must occur exactly once in each of the 9 3x3 sub-boxes of the grid.\n\nThe '.' character indicates empty cells.\n \nExample 1:\n\n\nInput: board = [[\"5\",\"3\",\".\",\".\",\"7\",\".\",\".\",\".\",\".\"],[\"6\",\".\",\".\",\"1\",\"9\",\"5\",\".\",\".\",\".\"],[\".\",\"9\",\"8\",\".\",\".\",\".\",\".\",\"6\",\".\"],[\"8\",\".\",\".\",\".\",\"6\",\".\",\".\",\".\",\"3\"],[\"4\",\".\",\".\",\"8\",\".\",\"3\",\".\",\".\",\"1\"],[\"7\",\".\",\".\",\".\",\"2\",\".\",\".\",\".\",\"6\"],[\".\",\"6\",\".\",\".\",\".\",\".\",\"2\",\"8\",\".\"],[\".\",\".\",\".\",\"4\",\"1\",\"9\",\".\",\".\",\"5\"],[\".\",\".\",\".\",\".\",\"8\",\".\",\".\",\"7\",\"9\"]]\nOutput: [[\"5\",\"3\",\"4\",\"6\",\"7\",\"8\",\"9\",\"1\",\"2\"],[\"6\",\"7\",\"2\",\"1\",\"9\",\"5\",\"3\",\"4\",\"8\"],[\"1\",\"9\",\"8\",\"3\",\"4\",\"2\",\"5\",\"6\",\"7\"],[\"8\",\"5\",\"9\",\"7\",\"6\",\"1\",\"4\",\"2\",\"3\"],[\"4\",\"2\",\"6\",\"8\",\"5\",\"3\",\"7\",\"9\",\"1\"],[\"7\",\"1\",\"3\",\"9\",\"2\",\"4\",\"8\",\"5\",\"6\"],[\"9\",\"6\",\"1\",\"5\",\"3\",\"7\",\"2\",\"8\",\"4\"],[\"2\",\"8\",\"7\",\"4\",\"1\",\"9\",\"6\",\"3\",\"5\"],[\"3\",\"4\",\"5\",\"2\",\"8\",\"6\",\"1\",\"7\",\"9\"]]\nExplanation: The input board is shown above and the only valid solution is shown below:\n\n\n\n \nConstraints:\n\nboard.length == 9\nboard[i].length == 9\nboard[i][j] is a digit or '.'.\nIt is guaranteed that the input board has only one solution.\n\n",
    "problem_solution": "class Solution:\n    def solveSudoku(self, board: List[List[str]]) -> None:\n        def dfs(k):\n            nonlocal ok\n            if k == len(t):\n                ok = True\n                return\n            i, j = t[k]\n            for v in range(9):\n                if row[i][v] == col[j][v] == block[i // 3][j // 3][v] == False:\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = True\n                    board[i][j] = str(v + 1)\n                    dfs(k + 1)\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = False\n                if ok:\n                    return\n\n        row = [[False] * 9 for _ in range(9)]\n        col = [[False] * 9 for _ in range(9)]\n        block = [[[False] * 9 for _ in range(3)] for _ in range(3)]\n        t = []\n        ok = False\n        for i in range(9):\n            for j in range(9):\n                if board[i][j] == '.':\n                    t.append((i, j))\n                else:\n                    v = int(board[i][j]) - 1\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = True\n        dfs(0)\n",
    "difficulty": "Hard",
    "tags": [
      "Array",
      "Hash Table",
      "Backtracking",
      "Matrix"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The code is not actually buggy: once ok becomes True, each stack frame returns immediately after the recursive call, so the final solution is not undone and the board remains solved.",
        "The bug explanation (solution being cleared by continued backtracking) does not match the actual control flow, so the hint targets a non-existent issue."
      ],
      "suggested_improvement": "Replace with a real failing bug (e.g., incorrect block indexing like block[i//3][j//3][v] with a wrongly shaped block array, or missing early-exit check before undoing), and adjust the hint to point to that specific failing behavior via questions."
    }
  },
  {
    "error_type": "off_by_one",
    "buggy_code": "class Solution:\n    def solveSudoku(self, board: List[List[str]]) -> None:\n        def dfs(k):\n            nonlocal ok\n            if k == len(t):\n                ok = True\n                return\n            i, j = t[k]\n            for v in range(1, 10):\n                if row[i][v] == col[j][v] == block[i // 3][j // 3][v] == False:\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = True\n                    board[i][j] = str(v)\n                    dfs(k + 1)\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = False\n                if ok:\n                    return\n\n        row = [[False] * 10 for _ in range(9)]\n        col = [[False] * 10 for _ in range(9)]\n        block = [[[False] * 10 for _ in range(3)] for _ in range(3)]\n        t = []\n        ok = False\n        for i in range(9):\n            for j in range(9):\n                if board[i][j] == '.':\n                    t.append((i, j))\n                else:\n                    v = int(board[i][j])\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = True\n        dfs(0)",
    "bug_explanation": "The student is using 1-indexed tracking arrays (size 10) and storing digits 1-9 directly as indices, but the arrays still have index 0 which is never used. While the arrays are sized correctly, the logic tries to access these indices directly without adjustment. More critically, when iterating v from 1 to 10 (range(1,10)), the loop tries values 1-9 but stores them at indices 1-9 in arrays that are 0-indexed. This creates confusion and the student isn't consistently using either 0-based or 1-based indexing throughout.",
    "socratic_hint": "Look at how you're using your tracking arrays. When you iterate through possible digit values, what indices are you using to access your arrays? Are you consistently using the same indexing scheme everywhere, or mixing zero-based and one-based indexing in different parts of your code?",
    "problem_id": 37,
    "problem_title": "Sudoku Solver",
    "problem_description": "Write a program to solve a Sudoku puzzle by filling the empty cells.\nA sudoku solution must satisfy all of the following rules:\n\nEach of the digits 1-9 must occur exactly once in each row.\nEach of the digits 1-9 must occur exactly once in each column.\nEach of the digits 1-9 must occur exactly once in each of the 9 3x3 sub-boxes of the grid.\n\nThe '.' character indicates empty cells.\n \nExample 1:\n\n\nInput: board = [[\"5\",\"3\",\".\",\".\",\"7\",\".\",\".\",\".\",\".\"],[\"6\",\".\",\".\",\"1\",\"9\",\"5\",\".\",\".\",\".\"],[\".\",\"9\",\"8\",\".\",\".\",\".\",\".\",\"6\",\".\"],[\"8\",\".\",\".\",\".\",\"6\",\".\",\".\",\".\",\"3\"],[\"4\",\".\",\".\",\"8\",\".\",\"3\",\".\",\".\",\"1\"],[\"7\",\".\",\".\",\".\",\"2\",\".\",\".\",\".\",\"6\"],[\".\",\"6\",\".\",\".\",\".\",\".\",\"2\",\"8\",\".\"],[\".\",\".\",\".\",\"4\",\"1\",\"9\",\".\",\".\",\"5\"],[\".\",\".\",\".\",\".\",\"8\",\".\",\".\",\"7\",\"9\"]]\nOutput: [[\"5\",\"3\",\"4\",\"6\",\"7\",\"8\",\"9\",\"1\",\"2\"],[\"6\",\"7\",\"2\",\"1\",\"9\",\"5\",\"3\",\"4\",\"8\"],[\"1\",\"9\",\"8\",\"3\",\"4\",\"2\",\"5\",\"6\",\"7\"],[\"8\",\"5\",\"9\",\"7\",\"6\",\"1\",\"4\",\"2\",\"3\"],[\"4\",\"2\",\"6\",\"8\",\"5\",\"3\",\"7\",\"9\",\"1\"],[\"7\",\"1\",\"3\",\"9\",\"2\",\"4\",\"8\",\"5\",\"6\"],[\"9\",\"6\",\"1\",\"5\",\"3\",\"7\",\"2\",\"8\",\"4\"],[\"2\",\"8\",\"7\",\"4\",\"1\",\"9\",\"6\",\"3\",\"5\"],[\"3\",\"4\",\"5\",\"2\",\"8\",\"6\",\"1\",\"7\",\"9\"]]\nExplanation: The input board is shown above and the only valid solution is shown below:\n\n\n\n \nConstraints:\n\nboard.length == 9\nboard[i].length == 9\nboard[i][j] is a digit or '.'.\nIt is guaranteed that the input board has only one solution.\n\n",
    "problem_solution": "class Solution:\n    def solveSudoku(self, board: List[List[str]]) -> None:\n        def dfs(k):\n            nonlocal ok\n            if k == len(t):\n                ok = True\n                return\n            i, j = t[k]\n            for v in range(9):\n                if row[i][v] == col[j][v] == block[i // 3][j // 3][v] == False:\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = True\n                    board[i][j] = str(v + 1)\n                    dfs(k + 1)\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = False\n                if ok:\n                    return\n\n        row = [[False] * 9 for _ in range(9)]\n        col = [[False] * 9 for _ in range(9)]\n        block = [[[False] * 9 for _ in range(3)] for _ in range(3)]\n        t = []\n        ok = False\n        for i in range(9):\n            for j in range(9):\n                if board[i][j] == '.':\n                    t.append((i, j))\n                else:\n                    v = int(board[i][j]) - 1\n                    row[i][v] = col[j][v] = block[i // 3][j // 3][v] = True\n        dfs(0)\n",
    "difficulty": "Hard",
    "tags": [
      "Array",
      "Hash Table",
      "Backtracking",
      "Matrix"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The code is not actually buggy: using size-10 boolean arrays and indexing digits 1-9 directly is a common and correct approach; index 0 being unused is fine.",
        "Claimed error type/off-by-one is incorrect; range(1,10) correctly iterates 1..9 and matches the tracking arrays.",
        "The hint targets an indexing inconsistency that doesn't exist, so it won't help diagnose a real failure."
      ],
      "suggested_improvement": "Replace this example with a genuinely failing Sudoku-solver bug (e.g., forgetting to reset board[i][j] to '.' on backtrack, incorrect block indexing, or returning too early), and update the hint to probe that specific failure mode."
    }
  },
  {
    "error_type": "logic_error",
    "buggy_code": "class Solution:\n    def isMatch(self, s: str, p: str) -> bool:\n        from functools import cache\n        \n        @cache\n        def dfs(i: int, j: int) -> bool:\n            if i >= len(s):\n                return j >= len(p) or (p[j] == \"*\" and dfs(i, j + 1))\n            if j >= len(p):\n                return False\n            if p[j] == \"*\":\n                return dfs(i + 1, j) or dfs(i, j + 1)\n            return (p[j] == \"?\" or s[i] == p[j]) and dfs(i + 1, j + 1)\n\n        return dfs(0, 0)",
    "bug_explanation": "When handling the asterisk wildcard, the code only explores two options: matching one character from s and staying at the same pattern position, or skipping the asterisk. It misses the third critical case where the asterisk matches one character AND we also advance in the pattern. This causes failures when the asterisk needs to match characters while there are more pattern characters ahead. For example, with s='aa' and p='*a', it will incorrectly return False.",
    "socratic_hint": "When a wildcard can match zero or more characters, how many different possibilities should you explore? Think about: using it to match nothing, using it to match something while keeping it available, and using it to match something while moving past it. Are you covering all these scenarios?",
    "problem_id": 44,
    "problem_title": "Wildcard Matching",
    "problem_description": "Given an input string (s) and a pattern (p), implement wildcard pattern matching with support for '?' and '*' where:\n\n'?' Matches any single character.\n'*' Matches any sequence of characters (including the empty sequence).\n\nThe matching should cover the entire input string (not partial).\n \nExample 1:\n\nInput: s = \"aa\", p = \"a\"\nOutput: false\nExplanation: \"a\" does not match the entire string \"aa\".\n\nExample 2:\n\nInput: s = \"aa\", p = \"*\"\nOutput: true\nExplanation: '*' matches any sequence.\n\nExample 3:\n\nInput: s = \"cb\", p = \"?a\"\nOutput: false\nExplanation: '?' matches 'c', but the second letter is 'a', which does not match 'b'.\n\n \nConstraints:\n\n0 <= s.length, p.length <= 2000\ns contains only lowercase English letters.\np contains only lowercase English letters, '?' or '*'.\n\n",
    "problem_solution": "class Solution:\n    def isMatch(self, s: str, p: str) -> bool:\n        @cache\n        def dfs(i: int, j: int) -> bool:\n            if i >= len(s):\n                return j >= len(p) or (p[j] == \"*\" and dfs(i, j + 1))\n            if j >= len(p):\n                return False\n            if p[j] == \"*\":\n                return dfs(i + 1, j) or dfs(i + 1, j + 1) or dfs(i, j + 1)\n            return (p[j] == \"?\" or s[i] == p[j]) and dfs(i + 1, j + 1)\n\n        return dfs(0, 0)\n",
    "difficulty": "Hard",
    "tags": [
      "Greedy",
      "Recursion",
      "String",
      "Dynamic Programming"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed bug is incorrect: for '*' the two explored branches (consume a character and stay on '*', or skip '*') are sufficient; there is no missing necessary case.",
        "The provided counterexample s='aa', p='*a' actually returns True with this code, so the code is not demonstrably wrong.",
        "Because the code appears correct, the hint targets a non-existent issue and is unlikely to help."
      ],
      "suggested_improvement": "Replace with a genuinely failing implementation (e.g., mishandling the base case when s is exhausted and remaining pattern has multiple '*', or incorrect memoization indices), and update the hint to focus on that real failure mode with a concrete failing test."
    }
  },
  {
    "error_type": "edge_case",
    "buggy_code": "class Solution:\n    def isNumber(self, s: str) -> bool:\n        n = len(s)\n        i = 0\n        if s[i] in '+-':\n            i += 1\n        if i == n:\n            return False\n        if s[i] == '.':\n            if i + 1 == n:\n                return False\n        dot = e = 0\n        j = i\n        while j < n:\n            if s[j] == '.':\n                if e or dot:\n                    return False\n                dot += 1\n            elif s[j] in 'eE':\n                if e or j == i or j == n - 1:\n                    return False\n                e += 1\n                if s[j + 1] in '+-':\n                    j += 1\n                    if j == n - 1:\n                        return False\n            elif not s[j].isnumeric():\n                return False\n            j += 1\n        return True",
    "bug_explanation": "The code fails to handle the case where a dot is immediately followed by the exponent notation (like '.e5' or '.E10'). The condition only checks if the dot is at the end of the string, but doesn't check if the next character after the dot is an exponent marker, which would make it invalid according to the problem definition.",
    "socratic_hint": "Your code handles some cases with the decimal point well. But what should happen when the decimal point appears right before an exponent marker? Think about whether that forms a valid decimal number according to the problem definition.",
    "problem_id": 65,
    "problem_title": "Valid Number",
    "problem_description": "Given a string s, return whether s is a valid number.\n\nFor example, all the following are valid numbers: \"2\", \"0089\", \"-0.1\", \"+3.14\", \"4.\", \"-.9\", \"2e10\", \"-90E3\", \"3e+7\", \"+6e-1\", \"53.5e93\", \"-123.456e789\", while the following are not valid numbers: \"abc\", \"1a\", \"1e\", \"e3\", \"99e2.5\", \"--6\", \"-+3\", \"95a54e53\".\nFormally, a valid number is defined using one of the following definitions:\n\nAn integer number followed by an optional exponent.\nA decimal number followed by an optional exponent.\n\nAn integer number is defined with an optional sign '-' or '+' followed by digits.\nA decimal number is defined with an optional sign '-' or '+' followed by one of the following definitions:\n\nDigits followed by a dot '.'.\nDigits followed by a dot '.' followed by digits.\nA dot '.' followed by digits.\n\nAn exponent is defined with an exponent notation 'e' or 'E' followed by an integer number.\nThe digits are defined as one or more digits.\n \nExample 1:\n\nInput: s = \"0\"\nOutput: true\n\nExample 2:\n\nInput: s = \"e\"\nOutput: false\n\nExample 3:\n\nInput: s = \".\"\nOutput: false\n\n \nConstraints:\n\n1 <= s.length <= 20\ns consists of only English letters (both uppercase and lowercase), digits (0-9), plus '+', minus '-', or dot '.'.\n\n",
    "problem_solution": "class Solution:\n    def isNumber(self, s: str) -> bool:\n        n = len(s)\n        i = 0\n        if s[i] in '+-':\n            i += 1\n        if i == n:\n            return False\n        if s[i] == '.' and (i + 1 == n or s[i + 1] in 'eE'):\n            return False\n        dot = e = 0\n        j = i\n        while j < n:\n            if s[j] == '.':\n                if e or dot:\n                    return False\n                dot += 1\n            elif s[j] in 'eE':\n                if e or j == i or j == n - 1:\n                    return False\n                e += 1\n                if s[j + 1] in '+-':\n                    j += 1\n                    if j == n - 1:\n                        return False\n            elif not s[j].isnumeric():\n                return False\n            j += 1\n        return True\n",
    "difficulty": "Hard",
    "tags": [
      "String"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed bug (rejecting cases like '.e5') is not actually a bug: the code already returns False for '.e5' because it detects 'e' at position i (j == i) and rejects it.",
        "Because the code is not actually buggy for the described edge case, the hint does not match a real failure mode."
      ],
      "suggested_improvement": "Find a real failing test case for this implementation (e.g., involving placement of signs/digits around '.', 'e', or empty integer/fraction parts) and rewrite the bug explanation and hint to target that specific observed failure."
    }
  },
  {
    "error_type": "off_by_one",
    "buggy_code": "class Solution:\n    def isNumber(self, s: str) -> bool:\n        n = len(s)\n        i = 0\n        if s[i] in '+-':\n            i += 1\n        if i == n:\n            return False\n        if s[i] == '.' and (i + 1 == n or s[i + 1] in 'eE'):\n            return False\n        dot = e = 0\n        j = i\n        while j < n:\n            if s[j] == '.':\n                if e or dot:\n                    return False\n                dot += 1\n            elif s[j] in 'eE':\n                if e or j == i or j == n - 1:\n                    return False\n                e += 1\n                if j + 1 < n and s[j + 1] in '+-':\n                    j += 1\n                    if j == n - 1:\n                        return False\n            elif not s[j].isnumeric():\n                return False\n            j += 1\n        return True",
    "bug_explanation": "The code adds a bounds check 'j + 1 < n' before checking if the next character after the exponent is a sign. This causes the code to accept invalid inputs like '3e+' because when the exponent is followed by a sign at the very end, the condition fails and doesn't increment j, so the sign character is checked in the next iteration and rejected. However, this also allows cases like '3e' to pass when they should fail, because the bounds check prevents looking ahead.",
    "socratic_hint": "When you check for a sign character after an exponent marker, you've added a boundary check before looking at the next character. What happens when the exponent marker is near the end of the string? Does your code correctly validate whether there are enough characters remaining to form a valid exponent?",
    "problem_id": 65,
    "problem_title": "Valid Number",
    "problem_description": "Given a string s, return whether s is a valid number.\n\nFor example, all the following are valid numbers: \"2\", \"0089\", \"-0.1\", \"+3.14\", \"4.\", \"-.9\", \"2e10\", \"-90E3\", \"3e+7\", \"+6e-1\", \"53.5e93\", \"-123.456e789\", while the following are not valid numbers: \"abc\", \"1a\", \"1e\", \"e3\", \"99e2.5\", \"--6\", \"-+3\", \"95a54e53\".\nFormally, a valid number is defined using one of the following definitions:\n\nAn integer number followed by an optional exponent.\nA decimal number followed by an optional exponent.\n\nAn integer number is defined with an optional sign '-' or '+' followed by digits.\nA decimal number is defined with an optional sign '-' or '+' followed by one of the following definitions:\n\nDigits followed by a dot '.'.\nDigits followed by a dot '.' followed by digits.\nA dot '.' followed by digits.\n\nAn exponent is defined with an exponent notation 'e' or 'E' followed by an integer number.\nThe digits are defined as one or more digits.\n \nExample 1:\n\nInput: s = \"0\"\nOutput: true\n\nExample 2:\n\nInput: s = \"e\"\nOutput: false\n\nExample 3:\n\nInput: s = \".\"\nOutput: false\n\n \nConstraints:\n\n1 <= s.length <= 20\ns consists of only English letters (both uppercase and lowercase), digits (0-9), plus '+', minus '-', or dot '.'.\n\n",
    "problem_solution": "class Solution:\n    def isNumber(self, s: str) -> bool:\n        n = len(s)\n        i = 0\n        if s[i] in '+-':\n            i += 1\n        if i == n:\n            return False\n        if s[i] == '.' and (i + 1 == n or s[i + 1] in 'eE'):\n            return False\n        dot = e = 0\n        j = i\n        while j < n:\n            if s[j] == '.':\n                if e or dot:\n                    return False\n                dot += 1\n            elif s[j] in 'eE':\n                if e or j == i or j == n - 1:\n                    return False\n                e += 1\n                if s[j + 1] in '+-':\n                    j += 1\n                    if j == n - 1:\n                        return False\n            elif not s[j].isnumeric():\n                return False\n            j += 1\n        return True\n",
    "difficulty": "Hard",
    "tags": [
      "String"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": false,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The claimed bug is incorrect: the code already rejects '3e' (via `j == n - 1`) and rejects '3e+' (it increments past '+' then rejects because it's the last char).",
        "Because there is no demonstrated failing test case, the hint targets a non-existent issue and is unlikely to help."
      ],
      "suggested_improvement": "Replace with a real failing case and align the hint to it. For example, this implementation incorrectly accepts strings with leading/trailing spaces (e.g., ' 1') and may mishandle some Unicode numerals due to `isnumeric()`. If the intended spec is LeetCode's, use `isdigit()` and/or strip spaces, and craft a Socratic hint around that actual failure."
    }
  },
  {
    "error_type": "edge_case",
    "buggy_code": "class Solution:\n    def isScramble(self, s1: str, s2: str) -> bool:\n        from functools import cache\n        \n        @cache\n        def dfs(i: int, j: int, k: int) -> bool:\n            if k == 1:\n                return s1[i] == s2[j]\n            for h in range(1, k):\n                if dfs(i, j, h) and dfs(i + h, j + h, k - h):\n                    return True\n                if dfs(i + h, j, k - h) and dfs(i, j + k - h, h):\n                    return True\n            return False\n\n        if len(s1) != len(s2):\n            return False\n        return dfs(0, 0, len(s1))",
    "bug_explanation": "While the length check is good, the code doesn't check if the two strings have the same character frequencies before recursing. If s1 and s2 have different characters (e.g., 'abc' vs 'def'), they can never be scrambles of each other, but the code will still explore all recursive possibilities, and more importantly, it will return False for cases like 'ab' and 'ba' which should return True, because it doesn't properly validate character composition early.",
    "socratic_hint": "Before recursively checking all possible splits, what fundamental property must two strings share in order for one to be a scramble of the other? Think about what remains invariant regardless of how you split and swap the parts.",
    "problem_id": 87,
    "problem_title": "Scramble String",
    "problem_description": "We can scramble a string s to get a string t using the following algorithm:\n\nIf the length of the string is 1, stop.\nIf the length of the string is > 1, do the following:\n\t\nSplit the string into two non-empty substrings at a random index, i.e., if the string is s, divide it to x and y where s = x + y.\nRandomly decide to swap the two substrings or to keep them in the same order. i.e., after this step, s may become s = x + y or s = y + x.\nApply step 1 recursively on each of the two substrings x and y.\n\n\n\nGiven two strings s1 and s2 of the same length, return true if s2 is a scrambled string of s1, otherwise, return false.\n \nExample 1:\n\nInput: s1 = \"great\", s2 = \"rgeat\"\nOutput: true\nExplanation: One possible scenario applied on s1 is:\n\"great\" --> \"gr/eat\" // divide at random index.\n\"gr/eat\" --> \"gr/eat\" // random decision is not to swap the two substrings and keep them in order.\n\"gr/eat\" --> \"g/r / e/at\" // apply the same algorithm recursively on both substrings. divide at random index each of them.\n\"g/r / e/at\" --> \"r/g / e/at\" // random decision was to swap the first substring and to keep the second substring in the same order.\n\"r/g / e/at\" --> \"r/g / e/ a/t\" // again apply the algorithm recursively, divide \"at\" to \"a/t\".\n\"r/g / e/ a/t\" --> \"r/g / e/ a/t\" // random decision is to keep both substrings in the same order.\nThe algorithm stops now, and the result string is \"rgeat\" which is s2.\nAs one possible scenario led s1 to be scrambled to s2, we return true.\n\nExample 2:\n\nInput: s1 = \"abcde\", s2 = \"caebd\"\nOutput: false\n\nExample 3:\n\nInput: s1 = \"a\", s2 = \"a\"\nOutput: true\n\n \nConstraints:\n\ns1.length == s2.length\n1 <= s1.length <= 30\ns1 and s2 consist of lowercase English letters.\n\n",
    "problem_solution": "class Solution:\n    def isScramble(self, s1: str, s2: str) -> bool:\n        @cache\n        def dfs(i: int, j: int, k: int) -> bool:\n            if k == 1:\n                return s1[i] == s2[j]\n            for h in range(1, k):\n                if dfs(i, j, h) and dfs(i + h, j + h, k - h):\n                    return True\n                if dfs(i + h, j, k - h) and dfs(i, j + k - h, h):\n                    return True\n            return False\n\n        return dfs(0, 0, len(s1))\n",
    "difficulty": "Hard",
    "tags": [
      "String",
      "Dynamic Programming"
    ],
    "validation": {
      "buggy_code_is_realistic": true,
      "buggy_code_is_actually_buggy": false,
      "hint_is_socratic": true,
      "hint_avoids_direct_fix": true,
      "hint_is_helpful": true,
      "hint_matches_bug": false,
      "overall_quality": "reject",
      "issues": [
        "The provided code is logically correct for the Scramble String problem; it should return True for cases like s1='ab', s2='ba'. Missing a character-frequency pruning check affects performance, not correctness.",
        "The bug explanation claims an incorrect output (false negative for 'ab' vs 'ba'), which is not true for this implementation, so the hint does not match an actual bug."
      ],
      "suggested_improvement": "If you want an edge_case correctness bug, modify the code so it can return True for strings with different character multisets (e.g., by adding an incorrect base case or missing length handling). If you want a performance/timeout issue instead, relabel the error type to something like 'performance' and adjust the explanation/hint to focus on pruning with an invariant (character counts) without claiming wrong outputs."
    }
  }
]